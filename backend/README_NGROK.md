# Настройка ngrok для работы с Figma

> ⚠️ **ВАЖНО:** Это **временное решение** только для разработки фронтенда!  
> Для production используйте реальный сервер с HTTPS, а не ngrok туннель.

## Проблема

Браузеры блокируют запросы к `localhost` из HTTPS-сайтов (например, Figma) из-за политики безопасности Private Network Access. Это не проблема CORS настроек Django, а ограничение браузера.

## Временное решение: Использование ngrok

Ngrok создаёт публичный HTTPS-туннель к вашему локальному серверу, что позволяет обойти это ограничение **во время разработки**. Это необходимо только для тестирования фронтенда в Figma до развёртывания на production сервере.

## Быстрый старт

1. **Убедитесь, что Django сервер запущен:**
   ```bash
   cd backend
   python3 manage.py runserver
   ```

2. **Запустите ngrok туннель:**
   ```bash
   cd backend
   ./start_ngrok.sh
   ```
   
   Или вручную:
   ```bash
   ngrok http 8000
   ```

3. **Скопируйте публичный HTTPS URL** (например: `https://abc123.ngrok-free.app`)

4. **Используйте этот URL в Figma** вместо `http://localhost:8000`
   - Базовый URL API: `https://abc123.ngrok-free.app/api/v1`
   - Endpoint логина: `https://abc123.ngrok-free.app/api/v1/auth/login/`

## Альтернативные способы запуска ngrok

### Вариант 1: Через веб-интерфейс
После запуска `ngrok http 8000` откройте http://localhost:4040 в браузере для просмотра URL и статистики.

### Вариант 2: Получить URL программно
```bash
curl http://localhost:4040/api/tunnels | grep -o '"public_url":"https://[^"]*"' | head -1
```

### Вариант 3: Использовать фиксированный домен (требует аккаунт ngrok)
```bash
ngrok http 8000 --domain=your-fixed-domain.ngrok-free.app
```

## Важные замечания

- ⚠️ **Это временное решение** только для разработки фронтенда в Figma
- ⚠️ **Ngrok URL меняется** при каждом перезапуске (если не используете фиксированный домен)
- 🔒 **Для production** используйте реальный сервер с HTTPS, а не ngrok
- 📝 **CORS настройки** в Django настроены для разработки (`CORS_ALLOW_ALL_ORIGINS = True`) и должны быть изменены для production
- 🚀 **После завершения разработки** ngrok больше не понадобится — фронтенд будет работать с production API

## Остановка ngrok

```bash
pkill ngrok
```

Или нажмите `Ctrl+C` в терминале, где запущен ngrok.

