# =============================================================================
# Backend Environment Variables — EXAMPLE
# =============================================================================
# Скопируйте этот файл в .env и заполните реальными значениями
# cp .env.example .env

# Telegram Bot
TELEGRAM_BOT_TOKEN=

# ElevenLabs (Scribe v2 — транскрибация голосовых)
ELEVENLABS_API_KEY=

# Sentry (мониторинг ошибок)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development

# PostgreSQL
DB_NAME=finans_assistant
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Redis (Celery broker)
CELERY_BROKER_URL=redis://localhost:6379/0

# MinIO / S3
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=worklog-media
AWS_S3_ENDPOINT_URL=http://localhost:9000

# Banking Encryption (шифрование банковских секретов)
# Сгенерируйте: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
BANK_ENCRYPTION_KEY=
