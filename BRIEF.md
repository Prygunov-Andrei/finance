# BRIEF — AC-Петя — Ф8D

## Где ты находишься

- **Рабочая директория:** `/Users/andrei_prygunov/obsidian/avgust/ERP_Avgust_ac_petya_f8d/`
- **Ветка:** `ac-rating/f8d` (от свежей `main` после Ф8C — все 8 фаз UI завершены).
- **Worktree:** изолированный.

## Кто ты

**AC-Петя** — backend AC Rating. Финальная фаза эпика Ф8: **очистка Django-admin**.

После твоего мержа — общий батч-деплой Ф8A+Ф8B+Ф8C+Ф8D на прод.

## Правила worktree

1. Не переключайся в другой checkout.
2. **НЕ пушь напрямую в `main`.** Только `ac-rating/f8d`.
3. `git fetch origin && git rebase origin/main` перед push.
4. **`backend/finans_assistant/urls.py`** — shared file. ОБЯЗАТЕЛЬНЫЙ пинг ISMeta-команде ДО коммита (через Андрея). Это критичная правка.
5. Не трогай ISMeta+Recognition.
6. Conventional Commits, маленькие коммиты.

## Особенность Ф8D

Эта фаза **меняет глобальное поведение** Django-admin для всего проекта. После твоего мержа `/admin/` будет показывать **только** AC Methodology + auth + logs. Все остальные модели (договоры, сметы, платежи, новости и т.д.) станут **недоступны через Django-admin**.

Это **намеренное** решение Андрея (Q-F8.4 + Q-F8.D + dialog 2026-04-26). ERP-команда уже использует основной ERP-UI на shadcn, не Django-admin. Но если ты найдёшь что какой-то рабочий процесс ERP-команды критически зависит от `/admin/<app>/<model>/` — пингни в чат до коммита.

**Backup-вариант** уже встроен: `/hvac-admin/` (строка 114 в urls.py) — клон с полным admin. ERP-команда может использовать его если нужен экстренный доступ к скрытым моделям. **НЕ трогай его** — оставь как есть. Только `/admin/` фильтруем.

## Что почитать ДО старта

1. `TASK.md` — детальное ТЗ.
2. `CLAUDE.md` в корне.
3. `ac-rating/tz/F8-admin-ui-rewrite.md` — Ф8D раздел (что оставить, что скрыть).
4. **Backend:**
   - `backend/finans_assistant/urls.py:113-114` — точка монтирования `admin.site.urls`.
   - `backend/ac_methodology/admin/` — admin classes для методики (переиспользовать).
   - Список всех `@admin.register(...)` декораторов в проекте — для понимания scope (`grep -rn "@admin.register" backend/ --include='*.py'`).

## Как сдавать

Отчёт Андрею: коммиты, что сделано, прогон, риски, ключевые файлы. **Особое внимание:** список моделей которые останутся в `/admin/` и список тех что скрыли. Не мерж сам.
