# 🚀 Production Deployment (документация переехала)

Актуальная деплой-документация теперь в `docs/deploy/PRODUCTION.md`.

Важно:
- не хранить в репозитории пароли/токены/ключи;
- использовать плейсхолдеры (`SERVER_IP`, `PRODUCTION_DOMAIN`, `TELEGRAM_BOT_TOKEN`) и заполнять реальные значения локально (например, в `docs/private/`, папка игнорируется git).
