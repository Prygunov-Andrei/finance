# Задания на разработку Фронтенда: Переписка

**Стек:** Next.js (App Router), TypeScript, TailwindCSS, Shadcn UI, Axios, React Query.

**Базовый URL API:** `https://finance.ngrok.app/api/v1` (или `NEXT_PUBLIC_API_URL`)

**Связанные документы:**
- `FRONTEND_TASKS_01_FOUNDATION.md` — Базовые этапы (Фундамент, Справочники)
- `FRONTEND_TASKS_06_CONTRACTS.md` — Договоры (переписка связана с договорами)
- `FRONTEND_TASKS_INDEX.md` — Индекс всех заданий
- `PROJECT.md` — Полная документация проекта

---

## Этап 32: Страница переписки

### Страница "Переписка" (`/communications`)

**API:** `GET /correspondence/`, `POST /correspondence/`, `PATCH /correspondence/{id}/`, `DELETE /correspondence/{id}/`

**Интерфейс данных:**
```typescript
interface Correspondence {
  id: number;
  contract: number; // ID договора
  contract_number?: string; // Read-only
  contract_name?: string; // Read-only
  type: 'incoming' | 'outgoing';
  category: 'уведомление' | 'претензия' | 'запрос' | 'ответ' | 'прочее';
  number: string;
  date: string; // YYYY-MM-DD
  status: 'новое' | 'в работе' | 'отвечено' | 'закрыто';
  subject: string;
  description?: string;
  file?: string; // URL файла
  related_to?: number; // ID связанного письма
  related_to_number?: string; // Read-only
  created_at: string;
  updated_at: string;
}
```

**UI:**
- Таблица переписки. Заголовки: Дата, Номер, Тип, Категория, Договор, Тема, Статус.
- Типы (Badge): 
  - `incoming` = "Входящее" (Blue, иконка ↓)
  - `outgoing` = "Исходящее" (Green, иконка ↑)
- Категории (Badge): 
  - `уведомление` = "Уведомление" (Gray)
  - `претензия` = "Претензия" (Red)
  - `запрос` = "Запрос" (Blue)
  - `ответ` = "Ответ" (Green)
  - `прочее` = "Прочее" (Gray)
- Статусы (Badge): 
  - `новое` = "Новое" (Yellow)
  - `в работе` = "В работе" (Blue)
  - `отвечено` = "Отвечено" (Green)
  - `закрыто` = "Закрыто" (Gray)
- Фильтры:
  - По договору: `?contract={id}`
  - По типу: `?type=incoming` или `?type=outgoing`
  - По категории: `?category=претензия`
  - По статусу: `?status=новое`
  - Поиск: `?search=...` (по номеру, теме, описанию)
- Кнопка "Создать письмо" открывает Dialog
- В таблице: клик по строке открывает детали письма
- В деталях: показывать цепочку писем (related_to), если есть

### Форма создания/редактирования письма

**Поля:**
- Договор* (Select) — обязательно
- Тип* (Select: Входящее / Исходящее) — обязательно
- Категория* (Select) — обязательно
- Номер* (Text input) — обязательно
- Дата* (Date picker) — обязательно
- Тема* (Text input) — обязательно
- Описание (Textarea)
- Связанное письмо (Select, опционально) — показывает список писем по договору
- Файл (File input, опционально)
- Статус (Select, по умолчанию "Новое")

### ✅ Чек-лист проверки
- [ ] Список переписки отображается корректно
- [ ] Создание письма работает
- [ ] Редактирование письма работает
- [ ] Удаление письма работает
- [ ] Фильтры работают
- [ ] Поиск работает
- [ ] Цепочка писем отображается в деталях

---

## Этап 33: Интеграция в договоры

### Вкладка "Переписка" в Договоре

**API:** `GET /correspondence/?contract={id}`

**UI:**
- Простой список (List View)
- Иконка стрелки (Входящее/Исходящее)
- Поля: Дата, Номер, Тема, Статус
- Кнопка "Добавить письмо" открывает форму (с предзаполненным договором)
- Клик по строке открывает детали письма

### ✅ Чек-лист проверки
- [ ] Вкладка "Переписка" отображается в договоре
- [ ] Список писем по договору отображается
- [ ] Создание письма из договора работает (договор предзаполнен)

---

## Навигация

Добавить в Sidebar:
- 📧 Переписка (`/communications`)

---

## Чек-лист финальной проверки

- [ ] Все CRUD операции работают
- [ ] Интеграция в договоры работает
- [ ] Фильтры и поиск работают
- [ ] Цепочка писем отображается
- [ ] Навигация обновлена
