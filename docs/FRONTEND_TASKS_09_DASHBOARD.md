# Задания на разработку Фронтенда: Dashboard и Аналитика

**Стек:** Next.js (App Router), TypeScript, TailwindCSS, Shadcn UI, Axios, React Query.

**Базовый URL API:** `https://finance.ngrok.app/api/v1` (или `NEXT_PUBLIC_API_URL`)

**Связанные документы:**
- `FRONTEND_TASKS_01_FOUNDATION.md` — Базовые этапы (Фундамент, Справочники)
- `FRONTEND_TASKS_02_OBJECTS.md` — Объекты строительства
- `FRONTEND_TASKS_06_CONTRACTS.md` — Договоры
- `FRONTEND_TASKS_07_PAYMENTS.md` — Платежи
- `FRONTEND_TASKS_INDEX.md` — Индекс всех заданий
- `PROJECT.md` — Полная документация проекта

---

## Этап 34: Базовый Dashboard

### Страница Dashboard (`/dashboard`)

**Структура:**
- Карточки с ключевыми метриками
- Графики
- Таблицы последних записей

### Метрики (Cards)

**API:** Использовать существующие endpoints для подсчёта

**Карточки:**
1. **Всего договоров**
   - API: `GET /contracts/?page_size=1` → `count`
   - Текст: "Всего договоров"
   - Значение: количество всех договоров (income + expense)

2. **Активных договоров**
   - API: `GET /contracts/?status=active&page_size=1` → `count`
   - Текст: "Активных договоров"
   - Значение: количество договоров со статусом `active`

3. **Общая сумма договоров**
   - API: `GET /contracts/` → суммировать `total_amount`
   - Текст: "Общая сумма договоров"
   - Значение: сумма всех договоров (форматировать как валюта)

4. **Дебиторская задолженность**
   - API: `GET /analytics/debt_summary/` → `total_receivable`
   - Текст: "Нам должны"
   - Значение: сумма по всем income договорам с положительным балансом
   - Цвет: Зеленый

5. **Кредиторская задолженность**
   - API: `GET /analytics/debt_summary/` → `total_payable`
   - Текст: "Мы должны"
   - Значение: сумма по всем expense договорам с положительным балансом
   - Цвет: Красный

6. **Остаток на счетах**
   - API: `GET /accounts/?is_active=true` → суммировать `current_balance`
   - Текст: "Остаток на счетах"
   - Значение: сумма всех активных счетов (форматировать как валюта)

### ✅ Чек-лист проверки
- [ ] Все карточки метрик отображаются
- [ ] Данные загружаются корректно
- [ ] Форматирование валюты работает

---

## Этап 35: Графики

### График 1: Денежный поток (Cash Flow)

**API:** `GET /analytics/cashflow/?period=year`

**Данные:** массив `{ month: string, income: number, expense: number }`

**UI:**
- Вид: Bar Chart
- Легенда: Приход, Расход
- Ось X: Месяцы
- Ось Y: Сумма (руб)
- Использовать библиотеку для графиков

### График 2: Структура задолженности

**API:** `GET /analytics/debt_summary/`

**Данные:** `{ total_receivable: number, total_payable: number }`

**UI:**
- Вид: Pie Chart или две карточки с большими цифрами
- Лейблы: "Нам должны" (Receivable, зеленый), "Мы должны" (Payable, красный)
- Использовать библиотеку для графиков

### График 3: Статусы договоров (опционально)

**API:** `GET /analytics/contracts_by_status/` (если реализовано)

**UI:**
- Вид: Pie Chart
- Показывать распределение договоров по статусам

### ✅ Чек-лист проверки
- [ ] График Cash-flow отображается
- [ ] График структуры задолженности отображается
- [ ] Данные обновляются при изменении данных

---

## Этап 36: Таблицы последних записей

### Таблица последних платежей

**API:** `GET /payments/?ordering=-payment_date&limit=10`

**UI:**
- Таблица с колонками: Дата, Тип, Договор, Сумма, Статус
- Заголовок: "Последние платежи"
- Клик по строке открывает детали платежа
- Кнопка "Все платежи" → `/payments`

### Таблица последних договоров

**API:** `GET /contracts/?ordering=-contract_date&limit=10`

**UI:**
- Таблица с колонками: Номер, Название, Контрагент, Сумма, Статус
- Заголовок: "Последние договоры"
- Клик по строке открывает детали договора
- Кнопка "Все договоры" → `/contracts`

### Таблица договоров с истекающими сроками (опционально)

**API:** Фильтр по `end_date` близко к текущей дате

**UI:**
- Таблица договоров, у которых `end_date` в ближайшие 30 дней
- Заголовок: "Договоры с истекающими сроками"
- Выделение: предупреждающий цвет (Orange)

### ✅ Чек-лист проверки
- [ ] Таблица последних платежей отображается
- [ ] Таблица последних договоров отображается
- [ ] Клик по строке открывает детали
- [ ] Кнопки "Все..." работают

---

## Этап 37: Дополнительные функции

### Экспорт данных

**Описание:** Добавить возможность экспорта основных списков в Excel.

**Реализация:**
- Использовать библиотеку для генерации Excel файлов на клиенте
- Или использовать endpoints на бэкенде (если реализовано)

**Списки для экспорта:**
- Договоры → `/contracts/export/` (если реализовано) или клиентский экспорт
- Платежи → `/payments/export/`
- Объекты → `/objects/export/`
- Контрагенты → `/counterparties/export/`
- ТКП → `/technical-proposals/export/`
- МП → `/mounting-proposals/export/`
- Сметы → `/estimates/export/`

**UI:**
- Кнопка "Экспорт в Excel" на каждой странице списка
- При клике: показать уведомление "Экспорт начат...", сгенерировать файл, скачать

### Глобальный поиск

**Описание:** Добавить глобальный поиск в Header для быстрого поиска по всем сущностям.

**Реализация:**

**Вариант 1: Поиск на бэкенде**
- API: `GET /search/?q={query}` (если реализовано)
- Результаты: `{ contracts: [], counterparties: [], objects: [], payments: [] }`

**Вариант 2: Поиск на клиенте**
- Поиск по каждой сущности отдельно
- Показывать результаты в Dropdown с группировкой по типам

**UI:**
- Поле поиска в Header (иконка поиска)
- При вводе показывать результаты в Dropdown
- Группировка: Договоры, Контрагенты, Объекты, Платежи, ТКП, МП
- Клик по результату открывает соответствующую страницу

### Уведомления и напоминания

**Описание:** Показывать уведомления о важных событиях и напоминаниях.

**Типы уведомлений:**

1. **Скоро истекающие рамочные договоры**
   - Проверка: `days_until_expiration < 30` и `status='active'`
   - Показывать Badge с количеством в меню "Договоры"

2. **Неоплаченные акты**
   - Проверка: `unpaid_amount > 0` и `status='signed'`
   - Показывать Badge с количеством в меню "Договоры"

3. **Договоры без привязанных ТКП/МП при статусе "В работе"**
   - Для income: нет ТКП или ТКП не approved
   - Для expense: нет МП или МП не approved
   - Показывать Badge с количеством в меню "Договоры"

4. **Проекты без проверок**
   - Проверка: `primary_check_done=false` или `secondary_check_done=false`
   - Показывать Badge с количеством в меню "Проекты"

**Реализация:**
- Использовать React Query для периодического обновления (каждые 5 минут)
- Показывать Badge с числом уведомлений рядом с пунктом меню
- При клике открывать страницу с фильтром по соответствующему условию

### Печать документов

**Описание:** Добавить возможность печати основных документов.

**Документы для печати:**
- Договоры (`/contracts/[id]/print`)
- Акты (`/contracts/acts/[id]/print`)
- ТКП (`/proposals/technical-proposals/[id]/print`)
- МП (`/proposals/mounting-proposals/[id]/print`)
- Рамочные договоры (`/contracts/framework-contracts/[id]/print`)

**Реализация:**
- Создать отдельные страницы для печати или использовать CSS `@media print`
- Кнопка "Печать" на каждой детальной странице
- Стилизация для печати: убрать меню, кнопки, оставить только содержимое

### Сохранение фильтров и состояния

**Описание:** Сохранять фильтры и состояние страниц для улучшения UX.

**Реализация:**

1. **Сохранение фильтров в URL**
   - Использовать URLSearchParams для сохранения всех фильтров
   - При загрузке страницы восстанавливать фильтры из URL
   - Позволяет делиться ссылками с применёнными фильтрами

2. **Сохранение в LocalStorage**
   - Сохранять последние фильтры для каждой страницы
   - При возврате на страницу восстанавливать фильтры
   - Опционально: настройка "Запомнить фильтры" (Checkbox)

3. **Сохранение сортировки и пагинации**
   - Сохранять текущую страницу пагинации
   - Сохранять сортировку по колонкам

### Массовые операции

**Описание:** Добавить возможность массовых операций для улучшения производительности.

**Операции:**

1. **Массовое создание платежей**
   - Выбрать несколько актов → создать заявки в реестр
   - UI: Dialog с выбором актов (Checkbox list), кнопка "Создать заявки"

2. **Массовое согласование заявок**
   - Выбрать несколько заявок в реестре → согласовать все
   - UI: Checkbox в таблице, кнопка "Согласовать выбранные"

3. **Массовое добавление работ в прайс-лист**
   - Выбрать несколько работ → добавить в прайс-лист
   - UI: Dialog с выбором работ (Checkbox list), кнопка "Добавить"

### Расширенная аналитика

**Описание:** Добавить дополнительные аналитические отчёты и графики.

**Отчёты:**

1. **Отчёт по контрагентам**
   - API: `GET /analytics/counterparties/` (если реализовано)
   - Показывать: сумма договоров, задолженность, количество договоров

2. **Отчёт по объектам**
   - API: `GET /analytics/objects/` (если реализовано)
   - Показывать: сумма договоров, маржинальность, статусы

3. **Отчёт по периодам**
   - API: `GET /analytics/periods/?start_date={date}&end_date={date}`
   - Показывать: доходы, расходы, прибыль по периодам

### ✅ Чек-лист проверки
- [ ] Экспорт в Excel работает для основных списков
- [ ] Глобальный поиск работает
- [ ] Уведомления отображаются корректно
- [ ] Печать документов работает
- [ ] Фильтры сохраняются в URL и LocalStorage
- [ ] Массовые операции работают
- [ ] Расширенная аналитика работает (если реализовано на бэкенде)

---

## Навигация

Dashboard уже добавлен в Sidebar:
- 🏠 Dashboard (`/dashboard`)

---

## Чек-лист финальной проверки

- [ ] Все метрики отображаются корректно
- [ ] Все графики работают
- [ ] Таблицы последних записей работают
- [ ] Экспорт данных работает
- [ ] Глобальный поиск работает
- [ ] Уведомления работают
- [ ] Печать документов работает
- [ ] Сохранение фильтров работает
