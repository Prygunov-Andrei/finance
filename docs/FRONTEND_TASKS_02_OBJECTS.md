# Задания на разработку Фронтенда: Объекты строительства

**Стек:** Next.js (App Router), TypeScript, TailwindCSS, Shadcn UI, Axios, React Query.

**Базовый URL API:** `https://finance.ngrok.app/api/v1` (или `NEXT_PUBLIC_API_URL`)

**Связанные документы:**
- `FRONTEND_TASKS_01_FOUNDATION.md` — Базовые этапы (Фундамент, Справочники)
- `FRONTEND_TASKS_06_CONTRACTS.md` — Договоры (связаны с объектами)
- `FRONTEND_TASKS_04_PROJECTS_ESTIMATES.md` — Проекты и Сметы (связаны с объектами)
- `FRONTEND_TASKS_05_TKP_MP.md` — ТКП и МП (связаны с объектами)
- `FRONTEND_TASKS_INDEX.md` — Индекс всех заданий
- `PROJECT.md` — Полная документация проекта

---

## Этап 3: Список объектов

### Страница списка объектов (`/objects`)

**API:** `GET /objects/`, `POST /objects/`, `PATCH /objects/{id}/`, `DELETE /objects/{id}/`

**Интерфейс данных:**
```typescript
interface ConstructionObject {
  id: number;
  name: string; // unique
  address: string;
  status: 'planned' | 'active' | 'completed' | 'suspended';
  start_date: string | null; // YYYY-MM-DD
  end_date: string | null;
  description?: string;
  contracts_count?: number; // Только в списке (ObjectListSerializer)
  created_at: string;
  updated_at: string;
}
```

**UI:**
- Grid сетка из карточек объектов. На карточке: Название крупно, адрес, статус (Badge), даты.
- Статусы (Badge): 
  - `planned` = "Планируется" (Gray)
  - `active` = "В работе" (Green)
  - `completed` = "Завершён" (Blue)
  - `suspended` = "Приостановлен" (Orange)
- Кнопка "Новый объект" → Modal Form.
- Поля формы:
  - Название* (обязательно, unique)
  - Адрес* (обязательно)
  - Статус (Select)
  - Дата начала (Date picker, опционально)
  - Дата окончания (Date picker, опционально)
  - Описание (Textarea, опционально)
- Фильтры:
  - По статусу: `?status={status}`
  - Поиск: `?search={text}` (по названию/адресу)
- Клик по карточке открывает детальную страницу объекта.

### ✅ Чек-лист проверки
- [ ] Список объектов отображается корректно
- [ ] Создание объекта работает
- [ ] Редактирование объекта работает
- [ ] Удаление объекта работает
- [ ] Фильтры по статусу работают
- [ ] Поиск работает

---

## Этап 4: Детальная страница объекта

### Страница деталей объекта (`/objects/[id]`)

**API:** 
- `GET /objects/{id}/` — детальная информация об объекте
- `GET /objects/{id}/cash_flow/` — денежный поток по объекту

**Структура страницы:**

**1. Шапка:**
- Название объекта (крупно)
- Адрес объекта
- Статус (Badge)
- Кнопки: Редактировать, Удалить

**2. Панель информации:**
- Дата начала (если указана)
- Дата окончания (если указана)
- Описание (если указано)
- Количество договоров (если доступно)

**3. Вкладки:**

#### Вкладка "Основное"
- Все поля объекта в виде Grid (ключ-значение)
- Кнопки редактирования

#### Вкладка "Договоры"
- API: `GET /contracts/?object={id}`
- Таблица договоров объекта
- Колонки: Номер, Название, Дата, Контрагент, Сумма, Статус
- Клик по строке открывает детальную страницу договора

#### Вкладка "Проекты"
- API: `GET /projects/?object={id}`
- Таблица проектов объекта
- Колонки: Шифр, Название, Стадия, Дата, Статус проверки
- Клик по строке открывает детальную страницу проекта

#### Вкладка "Сметы"
- API: `GET /estimates/?object={id}`
- Таблица смет объекта
- Колонки: Номер, Название, Компания, Статус, Сумма
- Клик по строке открывает детальную страницу сметы

#### Вкладка "ТКП"
- API: `GET /technical-proposals/?object={id}`
- Таблица ТКП объекта
- Колонки: Номер, Название, Дата, Компания, Статус, Сумма
- Клик по строке открывает детальную страницу ТКП

#### Вкладка "МП"
- API: `GET /mounting-proposals/?object={id}`
- Таблица МП объекта
- Колонки: Номер, Название, Дата, Исполнитель, Статус, Сумма
- Клик по строке открывает детальную страницу МП

#### Вкладка "Cash-flow"
- API: `GET /objects/{id}/cash_flow/?start_date={date}&end_date={date}`
- График денежного потока по объекту
- Данные: массив `{ date: string, income: number, expense: number, net: number }`
- Вид: Line Chart или Bar Chart
- Легенда: Приход, Расход, Чистый поток
- Фильтры:
  - Период (месяц/неделя/день)
  - Дата начала (Date picker)
  - Дата окончания (Date picker)

### ✅ Чек-лист проверки
- [ ] Детальная страница объекта открывается корректно
- [ ] Все вкладки отображаются
- [ ] Вкладка "Договоры" показывает список договоров объекта
- [ ] Вкладка "Проекты" показывает список проектов объекта
- [ ] Вкладка "Сметы" показывает список смет объекта
- [ ] Вкладка "ТКП" показывает список ТКП объекта
- [ ] Вкладка "МП" показывает список МП объекта
- [ ] Вкладка "Cash-flow" показывает график денежного потока
- [ ] Фильтры периода работают для графика Cash-flow

---

## Навигация

Добавить в Sidebar:
- 🏗️ Объекты (`/objects`)

---

## Чек-лист финальной проверки

- [ ] Все CRUD операции работают
- [ ] Детальная страница объекта отображает все связанные сущности
- [ ] График Cash-flow работает корректно
- [ ] Фильтры и поиск работают
- [ ] Навигация обновлена
