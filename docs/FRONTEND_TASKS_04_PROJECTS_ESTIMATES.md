# Задания на разработку Фронтенда: Проекты и Сметы

Этот документ содержит пошаговый план создания фронтенда для системы управления проектами и сметами. Каждый этап включает **полный, самодостаточный промпт** для ИИ-генератора кода. В промптах прописаны все необходимые API-контракты, типы данных и детали реализации.

**Общий стек:** Next.js (App Router), TypeScript, TailwindCSS, Shadcn UI, Axios, React Query.

**Связанные документы:**
- `FRONTEND_TASKS_01_FOUNDATION.md` — Базовые этапы (Фундамент, Справочники)
- `FRONTEND_TASKS_02_OBJECTS.md` — Объекты строительства
- `FRONTEND_TASKS_03_PRICELISTS.md` — Прайс-листы (используются в сметах)
- `FRONTEND_TASKS_05_TKP_MP.md` — ТКП и МП (используют сметы)
- `FRONTEND_TASKS_INDEX.md` — Индекс всех заданий
- `PROJECT.md` — Полная документация проекта

---

## Этап 10: Проекты (Project Management) 📁

### 🤖 Промпт для LLM
```text
Реализуем управление проектами с поддержкой версионирования, проверок и разрешений.
**Язык интерфейса: Русский.**

**1. Страница списка проектов (`/estimates/projects`)**
- API Endpoint: `GET /projects/`
- Интерфейс данных (для списка):
  ```typescript
  interface ProjectList {
    id: number;
    cipher: string; // "ПР-2025-001"
    name: string; // Название проекта
    date: string; // YYYY-MM-DD
    stage: 'П' | 'РД';
    stage_display: string; // "Проектная документация" | "Рабочая документация"
    object: number; // ID объекта
    object_name: string; // Read-only: название объекта
    is_approved_for_production: boolean;
    primary_check_done: boolean;
    secondary_check_done: boolean;
    version_number: number; // Read-only
    is_current: boolean; // Read-only
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - Таблица проектов. Заголовки: Шифр, Название, Объект, Дата, Стадия, Первичная проверка, Вторичная проверка, В производство, Версия.
  - **Фильтры:**
    - По объекту: `?object={id}` (Select из списка объектов)
    - По стадии: `?stage=П` или `?stage=РД` (Select: Все / П / РД)
    - В производство: `?is_approved_for_production=true` (Checkbox)
    - Первичная проверка: `?primary_check_done=true` (Checkbox)
    - Вторичная проверка: `?secondary_check_done=true` (Checkbox)
    - Поиск: `?search=...` (по шифру и названию)
  - Статусы (Badge):
    - Первичная проверка: ✓ (Green) / ✗ (Gray)
    - Вторичная проверка: ✓ (Green) / ✗ (Gray)
    - В производство: ✓ (Green) / ✗ (Gray)
  - По умолчанию показываются только актуальные версии (`is_current=true`).
  - Кнопка "Создать проект" открывает Dialog (см. ниже).
  - В таблице: клик по строке открывает детальную страницу.

**2. Форма создания/редактирования проекта**
- API Endpoint: `POST /projects/`, `PATCH /projects/{id}/`
- Интерфейс запроса:
  ```typescript
  interface ProjectCreateRequest {
    cipher: string; // Обязательно, уникальный
    name: string; // Обязательно
    date: string; // YYYY-MM-DD, обязательно
    stage: 'П' | 'РД'; // Обязательно
    object: number; // ID объекта, обязательно
    file: File; // ZIP-архив проекта, обязательно
    notes?: string; // Опционально
  }
  ```
- UI:
  - Шифр проекта (Text input) — обязательно, формат: "ПР-2025-001"
  - Название проекта (Text input) — обязательно
  - Дата проекта (Date picker) — обязательно
  - Стадия (Select: Проектная документация / Рабочая документация) — обязательно
  - Объект (Select из списка объектов) — обязательно
  - ZIP-архив проекта (File input, accept=".zip") — обязательно
  - Общие примечания (Textarea) — опционально
  - Валидация: при создании шифр+дата должны быть уникальными (бэкенд вернет ошибку 400).

**3. Страница деталей проекта (`/estimates/projects/[id]`)**
- API Endpoint: `GET /projects/{id}/`
- Интерфейс данных (для деталей):
  ```typescript
  interface ProjectDetail {
    id: number;
    cipher: string;
    name: string;
    date: string;
    stage: 'П' | 'РД';
    stage_display: string;
    object: number;
    object_name: string;
    file: string; // URL файла
    notes?: string;
    is_approved_for_production: boolean;
    production_approval_file?: string; // URL файла
    production_approval_date?: string; // YYYY-MM-DD
    primary_check_done: boolean;
    primary_check_by?: number; // ID пользователя
    primary_check_by_username?: string;
    primary_check_date?: string; // YYYY-MM-DD
    secondary_check_done: boolean;
    secondary_check_by?: number;
    secondary_check_by_username?: string;
    secondary_check_date?: string;
    parent_version?: number; // ID предыдущей версии
    version_number: number;
    is_current: boolean;
    project_notes: ProjectNote[]; // Массив замечаний
    created_at: string;
    updated_at: string;
  }
  
  interface ProjectNote {
    id: number;
    project: number;
    author: {
      id: number;
      username: string;
    };
    text: string;
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - **Карточка проекта:**
    - Основная информация: Шифр, Название, Дата, Стадия, Объект
    - Скачать файл проекта (кнопка с иконкой download)
    - Примечания (если есть)
  - **Секция "Проверки":**
    - Первичная проверка:
      - Статус: ✓ Выполнена / ✗ Не выполнена
      - Кто проверил: {username} (если выполнена)
      - Дата: {date} (если выполнена)
      - Кнопка "Отметить первичную проверку" (если не выполнена) → `POST /projects/{id}/primary-check/`
    - Вторичная проверка:
      - Аналогично первичной
      - Кнопка "Отметить вторичную проверку" → `POST /projects/{id}/secondary-check/`
  - **Секция "Разрешение в производство":**
    - Статус: ✓ Разрешено / ✗ Не разрешено
    - Дата получения разрешения: {date} (если разрешено)
    - Скачать файл разрешения (если есть)
    - Кнопка "Разрешить в производство" (если не разрешено) → `POST /projects/{id}/approve-production/` с файлом
      - Открывает Dialog для загрузки файла разрешения
  - **Секция "Замечания к проекту":**
    - Список замечаний (если есть)
    - Каждое замечание: Автор, Текст, Дата создания
    - Кнопка "Добавить замечание" → открывает Dialog:
      - Текст замечания (Textarea) — обязательно
      - Автор устанавливается автоматически из текущего пользователя
      - API: `POST /project-notes/` с `{ project: id, text: "..." }`
    - Кнопка "Редактировать" для каждого замечания (если автор = текущий пользователь)
      - API: `PATCH /project-notes/{id}/` с `{ text: "..." }`
    - Кнопка "Удалить" для каждого замечания (если автор = текущий пользователь)
      - API: `DELETE /project-notes/{id}/`
      - Показывать подтверждение перед удалением
  - **Секция "Версионирование":**
    - Текущая версия: {version_number}
    - Кнопка "Создать новую версию" → `POST /projects/{id}/create-version/`
      - Показывает подтверждение: "Создать новую версию проекта? Текущая версия будет помечена как неактуальная."
      - После создания открывает форму редактирования новой версии
    - Кнопка "История версий" → `GET /projects/{id}/versions/`
      - Открывает Dialog/Modal со списком всех версий
      - Каждая версия: Шифр, Версия, Дата, Актуальная (Badge)
      - Клик по версии открывает детали этой версии

**4. Страница замечаний к проекту (опционально, можно интегрировать в детали)**
- API Endpoint: `GET /project-notes/?project={id}`, `POST /project-notes/`, `PATCH /project-notes/{id}/`, `DELETE /project-notes/{id}/`
- Интерфейс данных: см. `ProjectNote` выше
- UI: (интегрировано в детали проекта, см. выше)
```

### ✅ Чек-лист проверки
- [ ] Создание проекта работает, валидация уникальности работает.
- [ ] Фильтрация по объекту, стадии, проверкам работает.
- [ ] Поиск по проектам работает.
- [ ] Отметка первичной и вторичной проверки работает.
- [ ] Разрешение в производство работает с загрузкой файла.
- [ ] Добавление/редактирование/удаление замечаний работает.
- [ ] Создание новой версии проекта работает.
- [ ] История версий отображается корректно.

---

## Этап 11: Сметы (Estimate Management) 💰

### 🤖 Промпт для LLM
```text
Реализуем управление сметами с разделами, подразделами и характеристиками.
**Язык интерфейса: Русский.**

**1. Страница списка смет (`/estimates/estimates`)**
- API Endpoint: `GET /estimates/`
- Интерфейс данных (для списка):
  ```typescript
  interface EstimateList {
    id: number;
    number: string; // "СМ-2025-001" (генерируется автоматически)
    name: string;
    object: number; // ID объекта
    object_name: string; // Read-only
    legal_entity: number; // ID нашей компании
    legal_entity_name: string; // Read-only
    status: 'draft' | 'in_progress' | 'checking' | 'approved' | 'sent' | 'agreed' | 'rejected';
    status_display: string; // Read-only
    with_vat: boolean;
    approved_by_customer: boolean;
    version_number: number; // Read-only
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - Таблица смет. Заголовки: Номер, Название, Объект, Компания, Статус, С НДС, Согласовано, Версия.
  - Статусы (Badge):
    - draft = "Черновик" (Gray)
    - in_progress = "В работе" (Blue)
    - checking = "На проверке" (Yellow)
    - approved = "Утверждена" (Green)
    - sent = "Отправлена Заказчику" (Blue)
    - agreed = "Согласована Заказчиком" (Green)
    - rejected = "Отклонена" (Red)
  - **Фильтры:**
    - По объекту: `?object={id}` (Select)
    - По компании: `?legal_entity={id}` (Select)
    - По статусу: `?status=draft` (Select)
    - Согласовано Заказчиком: `?approved_by_customer=true` (Checkbox)
    - Поиск: `?search=...` (по номеру и названию)
  - Кнопка "Создать смету" открывает Dialog (см. ниже).
  - В таблице: клик по строке открывает детальную страницу.

**2. Форма создания сметы**
- API Endpoint: `POST /estimates/`
- Интерфейс запроса:
  ```typescript
  interface EstimateCreateRequest {
    object: number; // ID объекта, обязательно
    legal_entity: number; // ID нашей компании, обязательно
    name: string; // Обязательно
    with_vat: boolean; // По умолчанию true
    vat_rate?: string; // Decimal string "20.00", по умолчанию 20.00
    projects?: number[]; // Массив ID проектов-оснований, опционально
    price_list?: number; // ID прайс-листа, опционально
    man_hours?: string; // Decimal string "0.00", опционально
    usd_rate?: string; // Decimal string, опционально
    eur_rate?: string; // Decimal string, опционально
    cny_rate?: string; // Decimal string, опционально
  }
  ```
- UI:
  - Объект (Select из списка объектов) — обязательно
  - Наша компания (Select из списка legal entities) — обязательно
  - Название сметы (Text input) — обязательно
  - С НДС (Checkbox, по умолчанию true)
  - Ставка НДС, % (Number input, по умолчанию 20.00, показывается только если "С НДС" = true)
  - Проекты-основания (Multi-select из списка проектов) — опционально
  - Прайс-лист для расчёта (Select из списка прайс-листов) — опционально
  - Человеко-часы (Number input) — опционально
  - Курсы валют (опционально):
    - Курс USD (Number input)
    - Курс EUR (Number input)
    - Курс CNY (Number input)
  - **Примечание:** Номер сметы генерируется автоматически на бэкенде (например, СМ-2025-001).
  - После создания автоматически создаются начальные характеристики: "Материалы" и "Работы" (автоматически рассчитываемые).

**3. Страница деталей сметы (`/estimates/estimates/[id]`)**
- API Endpoint: `GET /estimates/{id}/`
- Интерфейс данных (для деталей):
  ```typescript
  interface EstimateDetail {
    id: number;
    number: string;
    name: string;
    object: number;
    object_name: string;
    legal_entity: number;
    legal_entity_name: string;
    with_vat: boolean;
    vat_rate: string; // Decimal string
    projects: Array<{
      id: number;
      cipher: string;
      name: string;
    }>; // Read-only
    price_list?: number;
    price_list_name?: string;
    man_hours: string;
    usd_rate?: string;
    eur_rate?: string;
    cny_rate?: string;
    file?: string; // URL файла Excel
    status: string;
    status_display: string;
    approved_by_customer: boolean;
    approved_date?: string;
    created_by: number;
    created_by_username: string;
    checked_by?: number;
    checked_by_username?: string;
    approved_by?: number;
    approved_by_username?: string;
    parent_version?: number;
    version_number: number;
    sections: EstimateSection[]; // Массив разделов
    characteristics: EstimateCharacteristic[]; // Массив характеристик
    // Вычисляемые поля (read-only):
    total_materials_sale: string; // Decimal string
    total_works_sale: string;
    total_materials_purchase: string;
    total_works_purchase: string;
    total_sale: string;
    total_purchase: string;
    vat_amount: string;
    total_with_vat: string;
    profit_amount: string;
    profit_percent: string; // Decimal string
    created_at: string;
    updated_at: string;
  }
  
  interface EstimateSection {
    id: number;
    estimate: number;
    name: string;
    sort_order: number;
    subsections: EstimateSubsection[];
    // Вычисляемые поля (read-only):
    total_materials_sale: string;
    total_works_sale: string;
    total_materials_purchase: string;
    total_works_purchase: string;
    total_sale: string;
    total_purchase: string;
    created_at: string;
    updated_at: string;
  }
  
  interface EstimateSubsection {
    id: number;
    section: number;
    name: string;
    materials_sale: string; // Decimal string
    works_sale: string;
    materials_purchase: string;
    works_purchase: string;
    sort_order: number;
    // Вычисляемые поля (read-only):
    total_sale: string;
    total_purchase: string;
    created_at: string;
    updated_at: string;
  }
  
  interface EstimateCharacteristic {
    id: number;
    estimate: number;
    name: string; // "Материалы", "Работы", "Доставка", etc.
    purchase_amount: string; // Decimal string
    sale_amount: string;
    is_auto_calculated: boolean; // Read-only
    source_type: 'sections' | 'manual';
    source_type_display: string; // Read-only
    sort_order: number;
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - **Карточка сметы:**
    - Основная информация: Номер, Название, Объект, Компания, Статус
    - Проекты-основания: список проектов (если есть)
    - Прайс-лист: название (если указан)
    - Человеко-часы, курсы валют
    - Скачать Excel-файл (если есть)
  - **Секция "Разделы сметы":**
    - Аккордеон/Таблица разделов (использовать компонент Accordion)
    - Для каждого раздела:
      - Название раздела (заголовок аккордеона)
      - Итого по разделу: Материалы (продажа), Работы (продажа), Материалы (закупка), Работы (закупка), Итого продажа, Итого закупка (показывать в заголовке аккордеона)
      - Кнопка "Добавить подраздел" → открывает Dialog:
        - Название подраздела (Text input) — обязательно
        - Материалы — продажа (Number input, Decimal) — обязательно, >= 0
        - Работы — продажа (Number input, Decimal) — обязательно, >= 0
        - Материалы — закупка (Number input, Decimal) — обязательно, >= 0
        - Работы — закупка (Number input, Decimal) — обязательно, >= 0
        - Порядок сортировки (Number input, по умолчанию 0)
        - API: `POST /estimate-subsections/` с `{ section: id, name, materials_sale, works_sale, materials_purchase, works_purchase, sort_order }`
      - Список подразделов:
        - Таблица: Название, Материалы (продажа), Работы (продажа), Материалы (закупка), Работы (закупка), Итого продажа, Итого закупка
        - Кнопка "Редактировать" для каждого подраздела → открывает Dialog с формой редактирования
          - API: `PATCH /estimate-subsections/{id}/`
        - Кнопка "Удалить" для каждого подраздела (с подтверждением)
          - API: `DELETE /estimate-subsections/{id}/`
          - После удаления автоматически обновляются характеристики "Материалы" и "Работы"
    - Кнопка "Добавить раздел" → открывает Dialog:
      - Название раздела (Text input) — обязательно
      - Порядок сортировки (Number input, по умолчанию 0)
      - API: `POST /estimate-sections/` с `{ estimate: id, name, sort_order }`
    - Кнопка "Редактировать раздел" для каждого раздела
    - Кнопка "Удалить раздел" для каждого раздела (с подтверждением, удаляет все подразделы)
  - **Секция "Характеристики сметы":**
    - Таблица характеристик. Заголовки: Название, Закупка, Продажа, Источник, Авто.
    - Автоматически рассчитываемые характеристики (is_auto_calculated=true):
      - "Материалы" — рассчитывается из разделов (total_materials_sale/purchase)
      - "Работы" — рассчитывается из разделов (total_works_sale/purchase)
      - Показывать Badge "Авто" (Green)
      - Нельзя редактировать вручную (disabled)
    - Ручные характеристики:
      - Можно редактировать суммы
      - При редактировании теряется связь с автоматическим расчётом (is_auto_calculated становится false)
    - Кнопка "Добавить характеристику" → открывает Dialog:
      - Название (Text input) — обязательно
      - Сумма закупки (Number input, Decimal) — обязательно, >= 0
      - Сумма продажи (Number input, Decimal) — обязательно, >= 0
      - API: `POST /estimate-characteristics/` с `{ estimate: id, name, purchase_amount, sale_amount, source_type: 'manual' }`
    - Кнопка "Редактировать" для каждой характеристики → открывает Dialog:
      - Название (Text input, read-only для автоматических)
      - Сумма закупки (Number input, Decimal)
      - Сумма продажи (Number input, Decimal)
      - **Важно:** При редактировании автоматической характеристики она теряет связь с автоматическим расчётом
      - API: `PATCH /estimate-characteristics/{id}/`
    - Кнопка "Удалить" для каждой характеристики (кроме автоматических "Материалы" и "Работы")
      - API: `DELETE /estimate-characteristics/{id}/`
      - Показывать подтверждение перед удалением
  - **Секция "Итоги":**
    - Таблица итогов:
      - Материалы (продажа): {total_materials_sale}
      - Работы (продажа): {total_works_sale}
      - **Итого продажа:** {total_sale} (Bold)
      - Материалы (закупка): {total_materials_purchase}
      - Работы (закупка): {total_works_purchase}
      - **Итого закупка:** {total_purchase} (Bold)
      - **Прибыль:** {profit_amount} ({profit_percent}%) (Bold, Green если > 0, Red если < 0)
      - НДС: {vat_amount} (показывается только если with_vat=true)
      - **Итого с НДС:** {total_with_vat} (Bold, показывается только если with_vat=true)
  - **Секция "Версионирование":**
    - Текущая версия: {version_number}
    - Кнопка "Создать новую версию" → `POST /estimates/{id}/create-version/`
      - Показывает подтверждение
      - После создания открывает детали новой версии
    - Кнопка "История версий" → `GET /estimates/{id}/versions/`
  - **Секция "Действия":**
    - Кнопка "Создать монтажную смету" → `POST /estimates/{id}/create-mounting-estimate/`
      - Показывает подтверждение
      - После создания открывает детали созданной монтажной сметы
    - Кнопка "Обновить статус" (Select/Dropdown):
      - Изменяет статус сметы через `PATCH /estimates/{id}/` с `{ status: '...' }`
      - Доступные статусы: Черновик, В работе, На проверке, Утверждена, Отправлена Заказчику, Согласована Заказчиком, Отклонена
    - Кнопка "Отметить согласованной Заказчиком" (если статус позволяет):
      - `PATCH /estimates/{id}/` с `{ approved_by_customer: true, approved_date: today }`
      - Показывать Dialog для подтверждения
    - Кнопка "Редактировать смету" → открывает форму редактирования:
      - Можно изменить: название, объект, компанию, НДС, ставку НДС, проекты, прайс-лист, человеко-часы, курсы валют
      - API: `PATCH /estimates/{id}/`

**4. Управление разделами и подразделами**
- API Endpoints:
  - `GET /estimate-sections/?estimate={id}` — список разделов сметы
  - `POST /estimate-sections/` — создать раздел
  - `PATCH /estimate-sections/{id}/` — обновить раздел
  - `DELETE /estimate-sections/{id}/` — удалить раздел
  - `GET /estimate-subsections/?section={id}` или `?estimate={id}` — список подразделов
  - `POST /estimate-subsections/` — создать подраздел
  - `PATCH /estimate-subsections/{id}/` — обновить подраздел
  - `DELETE /estimate-subsections/{id}/` — удалить подраздел
- UI: (интегрировано в детали сметы, см. выше)
- **Важно:** При изменении подраздела автоматически обновляются характеристики "Материалы" и "Работы" (через сигнал на бэкенде).
```

### ✅ Чек-лист проверки
- [ ] Создание сметы работает, номер генерируется автоматически.
- [ ] Фильтрация по объекту, компании, статусу работает.
- [ ] Поиск по сметам работает.
- [ ] Добавление/редактирование/удаление разделов работает.
- [ ] Добавление/редактирование/удаление подразделов работает.
- [ ] Автоматические характеристики обновляются при изменении подразделов.
- [ ] Ручные характеристики можно добавлять и редактировать.
- [ ] Итоги рассчитываются корректно.
- [ ] Создание новой версии сметы работает.
- [ ] Создание монтажной сметы из обычной сметы работает.

---

## Этап 12: Монтажные сметы (Mounting Estimate Management) 🔧

### 🤖 Промпт для LLM
```text
Реализуем управление монтажными сметами (упрощёнными сметами для Исполнителей).
**Язык интерфейса: Русский.**

**1. Страница списка монтажных смет (`/estimates/mounting-estimates`)**
- API Endpoint: `GET /mounting-estimates/`
- Интерфейс данных (для списка):
  ```typescript
  interface MountingEstimateList {
    id: number;
    number: string; // "МС-2025-001" (генерируется автоматически)
    name: string;
    object: number; // ID объекта
    object_name: string; // Read-only
    source_estimate?: {
      id: number;
      number: string;
      name: string;
    }; // Read-only, если создана из обычной сметы
    total_amount: string; // Decimal string
    man_hours: string; // Decimal string
    status: 'draft' | 'sent' | 'approved' | 'rejected';
    status_display: string; // Read-only
    agreed_counterparty?: number; // ID контрагента
    agreed_date?: string; // YYYY-MM-DD
    version_number: number; // Read-only
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - Таблица монтажных смет. Заголовки: Номер, Название, Объект, Исходная смета, Сумма, Статус, Согласовано с, Версия.
  - Статусы (Badge):
    - draft = "Черновик" (Gray)
    - sent = "Отправлена" (Blue)
    - approved = "Согласована" (Green)
    - rejected = "Отклонена" (Red)
  - **Фильтры:**
    - По объекту: `?object={id}` (Select)
    - По исходной смете: `?source_estimate={id}` (Select)
    - По статусу: `?status=draft` (Select)
    - По Исполнителю: `?agreed_counterparty={id}` (Select)
    - Поиск: `?search=...` (по номеру и названию)
  - Кнопка "Создать монтажную смету" открывает Dialog (см. ниже).
  - Кнопка "Создать из сметы" открывает Dialog выбора сметы (см. ниже).
  - В таблице: клик по строке открывает детальную страницу.

**2. Форма создания монтажной сметы**
- API Endpoint: `POST /mounting-estimates/`
- Интерфейс запроса:
  ```typescript
  interface MountingEstimateCreateRequest {
    name: string; // Обязательно
    object: number; // ID объекта, обязательно
    source_estimate?: number; // ID исходной сметы, опционально
    total_amount: string; // Decimal string, обязательно
    man_hours?: string; // Decimal string, опционально, по умолчанию "0.00"
    status?: 'draft' | 'sent' | 'approved' | 'rejected'; // По умолчанию 'draft'
    file?: File; // Excel-файл, опционально
  }
  ```
- UI:
  - Название (Text input) — обязательно
  - Объект (Select из списка объектов) — обязательно
  - Исходная смета (Select из списка смет, опционально)
  - Итоговая сумма без НДС (Number input, Decimal) — обязательно, > 0
  - Человеко-часы (Number input, Decimal) — опционально
  - Статус (Select) — опционально, по умолчанию "Черновик"
  - Excel-файл (File input, accept=".xlsx,.xls") — опционально
  - **Примечание:** Номер генерируется автоматически на бэкенде (например, МС-2025-001).

**3. Форма создания монтажной сметы из обычной сметы**
- API Endpoint: `POST /mounting-estimates/from-estimate/`
- Интерфейс запроса:
  ```typescript
  interface MountingEstimateCreateFromEstimateRequest {
    estimate_id: number; // ID сметы, обязательно
  }
  ```
- UI:
  - Диалог выбора сметы:
    - Select из списка смет (показывать номер и название)
    - Кнопка "Создать"
  - После создания:
    - Название: "Монтажная смета к {название_сметы}"
    - Объект: копируется из сметы
    - Исходная смета: устанавливается выбранная смета
    - Итоговая сумма: копируется total_works_purchase из сметы
    - Человеко-часы: копируются из сметы
    - Статус: "Черновик"
  - После успешного создания открывается детальная страница созданной монтажной сметы.

**4. Страница деталей монтажной сметы (`/estimates/mounting-estimates/[id]`)**
- API Endpoint: `GET /mounting-estimates/{id}/`
- Интерфейс данных (для деталей):
  ```typescript
  interface MountingEstimateDetail {
    id: number;
    number: string;
    name: string;
    object: number;
    object_name: string;
    source_estimate?: {
      id: number;
      number: string;
      name: string;
    };
    total_amount: string; // Decimal string
    man_hours: string; // Decimal string
    file?: string; // URL файла Excel
    status: string;
    status_display: string;
    agreed_counterparty?: number;
    agreed_counterparty_detail?: {
      id: number;
      name: string;
      short_name: string;
    }; // Read-only
    agreed_date?: string; // YYYY-MM-DD
    created_by: number;
    created_by_username: string;
    parent_version?: number;
    version_number: number;
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - **Карточка монтажной сметы:**
    - Основная информация: Номер, Название, Объект
    - Исходная смета: ссылка на смету (если есть)
    - Итоговая сумма: {total_amount} руб. (Bold, крупный шрифт)
    - Человеко-часы: {man_hours}
    - Статус (Badge)
    - Скачать Excel-файл (если есть)
  - **Секция "Согласование":**
    - Согласовано с Исполнителем: {название контрагента} (если согласовано)
    - Дата согласования: {date} (если согласовано)
    - Кнопка "Согласовать с Исполнителем" (если не согласовано) → открывает Dialog:
      - Выбор контрагента (Select из списка контрагентов типа "Исполнитель/Поставщик" или "Заказчик и Исполнитель")
      - API: `POST /mounting-estimates/{id}/agree/` с `{ counterparty_id: id }`
      - После успешного согласования статус меняется на "Согласована"
  - **Секция "Версионирование":**
    - Текущая версия: {version_number}
    - Кнопка "Создать новую версию" → `POST /mounting-estimates/{id}/create-version/`
  - **Секция "Действия":**
    - Кнопка "Обновить статус" (Select/Dropdown):
      - Доступные статусы: Черновик, Отправлена, Согласована, Отклонена
      - API: `PATCH /mounting-estimates/{id}/` с `{ status: '...' }`
    - Кнопка "Редактировать" (открывает форму редактирования):
      - Можно изменить: название, объект, исходную смету, итоговую сумму, человеко-часы, статус, файл
      - API: `PATCH /mounting-estimates/{id}/`
```

### ✅ Чек-лист проверки
- [ ] Создание монтажной сметы работает, номер генерируется автоматически.
- [ ] Создание монтажной сметы из обычной сметы работает корректно.
- [ ] Фильтрация по объекту, исходной смете, статусу работает.
- [ ] Поиск по монтажным сметам работает.
- [ ] Согласование с Исполнителем работает, валидация типа контрагента работает.
- [ ] Создание новой версии монтажной сметы работает.
- [ ] Обновление статуса работает.

---

## Общие требования к реализации

### Технические детали

1. **Управление состоянием и кэшированием:**
   - Использовать библиотеку для управления состоянием и кэшированием API запросов
   - Настроить автоматическую инвалидацию кэша после мутаций
   - Обновлять списки после создания/обновления/удаления записей

2. **Обработка файлов:**
   - Для загрузки файлов использовать `FormData` и `multipart/form-data`
   - Для скачивания файлов использовать прямые ссылки на файлы или создать endpoint для скачивания

3. **Валидация форм:**
   - Реализовать валидацию форм на клиенте и сервере
   - Показывать ошибки валидации под каждым полем
   - Обрабатывать ошибки от бэкенда (400, 401, 403, 404, 500)

4. **UI/UX:**
   - Использовать компоненты UI библиотеки: Table, Dialog, Form, Input, Select, Button, Badge, Card, Tabs, Accordion
   - Показывать уведомления при успешных операциях и ошибках
   - Использовать индикаторы загрузки (Skeleton или Spinner)
   - Реализовать оптимистичные обновления где возможно

5. **Навигация:**
   - Добавить пункты меню в Sidebar:
     - 📁 Проекты (`/estimates/projects`)
     - 💰 Сметы (`/estimates/estimates`)
     - 🔧 Монтажные сметы (`/estimates/mounting-estimates`)
   - Или создать раздел "Сметы" с подменю:
     - Проекты
     - Сметы
     - Монтажные сметы

6. **Типы данных:**
   - Создать файл с TypeScript интерфейсами для всех сущностей
   - Использовать строгую типизацию для всех API запросов и ответов

7. **API клиент:**
   - Создать файл с функциями для всех API endpoints
   - Использовать существующий HTTP клиент из проекта

### Структура файлов

**Рекомендуемая структура:**
- Страницы: `app/estimates/projects/`, `app/estimates/estimates/`, `app/estimates/mounting-estimates/`
- Компоненты: `components/estimates/` (формы, детали, списки)
- API функции: `lib/api/estimates.ts`
- Типы данных: `types/estimates.ts`

### Пример типов данных (types/estimates.ts)

```typescript
// Projects
export interface ProjectList {
  id: number;
  cipher: string;
  name: string;
  date: string;
  stage: 'П' | 'РД';
  stage_display: string;
  object: number;
  object_name: string;
  is_approved_for_production: boolean;
  primary_check_done: boolean;
  secondary_check_done: boolean;
  version_number: number;
  is_current: boolean;
  created_at: string;
  updated_at: string;
}

export interface ProjectDetail extends ProjectList {
  file: string;
  notes?: string;
  production_approval_file?: string;
  production_approval_date?: string;
  primary_check_by?: number;
  primary_check_by_username?: string;
  primary_check_date?: string;
  secondary_check_by?: number;
  secondary_check_by_username?: string;
  secondary_check_date?: string;
  parent_version?: number;
  project_notes: ProjectNote[];
}

export interface ProjectNote {
  id: number;
  project: number;
  author: {
    id: number;
    username: string;
  };
  text: string;
  created_at: string;
  updated_at: string;
}

// Estimates
export interface EstimateList {
  id: number;
  number: string;
  name: string;
  object: number;
  object_name: string;
  legal_entity: number;
  legal_entity_name: string;
  status: 'draft' | 'in_progress' | 'checking' | 'approved' | 'sent' | 'agreed' | 'rejected';
  status_display: string;
  with_vat: boolean;
  approved_by_customer: boolean;
  version_number: number;
  created_at: string;
  updated_at: string;
}

export interface EstimateDetail extends EstimateList {
  vat_rate: string;
  projects: Array<{
    id: number;
    cipher: string;
    name: string;
  }>;
  price_list?: number;
  price_list_name?: string;
  man_hours: string;
  usd_rate?: string;
  eur_rate?: string;
  cny_rate?: string;
  file?: string;
  approved_date?: string;
  created_by: number;
  created_by_username: string;
  checked_by?: number;
  checked_by_username?: string;
  approved_by?: number;
  approved_by_username?: string;
  parent_version?: number;
  sections: EstimateSection[];
  characteristics: EstimateCharacteristic[];
  total_materials_sale: string;
  total_works_sale: string;
  total_materials_purchase: string;
  total_works_purchase: string;
  total_sale: string;
  total_purchase: string;
  vat_amount: string;
  total_with_vat: string;
  profit_amount: string;
  profit_percent: string;
}

export interface EstimateSection {
  id: number;
  estimate: number;
  name: string;
  sort_order: number;
  subsections: EstimateSubsection[];
  total_materials_sale: string;
  total_works_sale: string;
  total_materials_purchase: string;
  total_works_purchase: string;
  total_sale: string;
  total_purchase: string;
  created_at: string;
  updated_at: string;
}

export interface EstimateSubsection {
  id: number;
  section: number;
  name: string;
  materials_sale: string;
  works_sale: string;
  materials_purchase: string;
  works_purchase: string;
  sort_order: number;
  total_sale: string;
  total_purchase: string;
  created_at: string;
  updated_at: string;
}

export interface EstimateCharacteristic {
  id: number;
  estimate: number;
  name: string;
  purchase_amount: string;
  sale_amount: string;
  is_auto_calculated: boolean;
  source_type: 'sections' | 'manual';
  source_type_display: string;
  sort_order: number;
  created_at: string;
  updated_at: string;
}

// Mounting Estimates
export interface MountingEstimateList {
  id: number;
  number: string;
  name: string;
  object: number;
  object_name: string;
  source_estimate?: {
    id: number;
    number: string;
    name: string;
  };
  total_amount: string;
  man_hours: string;
  status: 'draft' | 'sent' | 'approved' | 'rejected';
  status_display: string;
  agreed_counterparty?: number;
  agreed_date?: string;
  version_number: number;
  created_at: string;
  updated_at: string;
}

export interface MountingEstimateDetail extends MountingEstimateList {
  file?: string;
  created_by: number;
  created_by_username: string;
  agreed_counterparty_detail?: {
    id: number;
    name: string;
    short_name: string;
  };
  parent_version?: number;
}
```

---

## Чек-лист финальной проверки

- [ ] Все страницы созданы и доступны через навигацию
- [ ] Все CRUD операции работают корректно
- [ ] Фильтрация и поиск работают на всех страницах
- [ ] Версионирование работает для проектов и смет
- [ ] Замечания к проектам работают (добавление/редактирование/удаление)
- [ ] Разделы и подразделы смет работают корректно
- [ ] Автоматические характеристики обновляются при изменении подразделов
- [ ] Итоги смет рассчитываются и отображаются корректно
- [ ] Создание монтажной сметы из обычной сметы работает
- [ ] Согласование монтажной сметы работает
- [ ] Загрузка и скачивание файлов работает
- [ ] Обработка ошибок работает (показываются понятные сообщения)
- [ ] Все формы валидируются корректно
- [ ] Toast уведомления показываются при успешных операциях
- [ ] UI соответствует дизайн-системе (Shadcn UI)

---

## Примечания

1. **Версионирование:**
   - При создании новой версии старая версия помечается как `is_current=false`
   - Новая версия получает `version_number++` и `is_current=true`
   - Для проектов: новая версия имеет ту же дату (но можно изменить), для смет: новый номер

2. **Автоматические характеристики:**
   - "Материалы" и "Работы" рассчитываются автоматически из разделов
   - При ручном редактировании характеристики теряется связь с автоматическим расчётом
   - Удалять автоматические характеристики нельзя

3. **Создание монтажной сметы из сметы:**
   - Копируется только `total_works_purchase` (работы — закупка)
   - Копируются `man_hours`
   - Создаётся связь с исходной сметой

4. **Согласование монтажной сметы:**
   - Контрагент должен быть типа "Исполнитель/Поставщик" или "Заказчик и Исполнитель"
   - Бэкенд вернёт ошибку 400, если тип контрагента не подходит

5. **Файлы:**
   - Проекты: обязательный ZIP-архив при создании
   - Сметы: опциональный Excel-файл
   - Монтажные сметы: опциональный Excel-файл
   - Разрешения проектов: обязательный файл при разрешении в производство

---

## Этап 13: Интеграция в меню и навигация 🧭

### 🤖 Промпт для LLM
```text
Добавить раздел "Проекты и Сметы" в основное меню приложения.
**Язык интерфейса: Русский.**

**1. Обновление Sidebar (Левое меню)**
- Добавить новый пункт меню: "📁 Проекты и Сметы" (`/estimates`)
- Подменю (при клике или hover):
  - "📁 Проекты" (`/estimates/projects`)
  - "💰 Сметы" (`/estimates/estimates`)
  - "🔧 Монтажные сметы" (`/estimates/mounting-estimates`)

**2. Breadcrumbs**
- На всех страницах проектов и смет показывать breadcrumbs:
  - Главная → Проекты и Сметы → [Текущая страница]
  - Например: Главная → Проекты и Сметы → Проекты → ПР-2025-001
  - Например: Главная → Проекты и Сметы → Сметы → СМ-2025-001

**3. Навигация между связанными сущностями**
- В таблице проектов: клик по объекту открывает страницу объектов с фильтром по этому объекту
- В детальной странице проекта: ссылка на объект (открывает детали объекта)
- В таблице смет: клик по объекту открывает страницу объектов
- В детальной странице сметы:
  - Ссылки на проекты-основания (открывают детали проектов)
  - Ссылка на прайс-лист (если указан, открывает детали прайс-листа)
  - Ссылка на объект (открывает детали объекта)
- В таблице монтажных смет:
  - Ссылка на исходную смету (если есть, открывает детали сметы)
  - Ссылка на объект (открывает детали объекта)
- В детальной странице монтажной сметы:
  - Ссылка на исходную смету (если есть)
  - Ссылка на объект
  - Ссылка на контрагента (если согласовано)
```

### ✅ Чек-лист проверки
- [ ] Раздел "Проекты и Сметы" добавлен в меню.
- [ ] Навигация между страницами работает.
- [ ] Breadcrumbs отображаются корректно.
- [ ] Ссылки на связанные сущности работают.

---

## Дополнительные улучшения (опционально) ✨

### 1. Экспорт сметы в Excel
- Добавить возможность экспорта сметы в Excel-файл (если бэкенд поддерживает).
- Кнопка "Экспорт в Excel" на странице деталей сметы.

### 2. Массовое создание подразделов
- Добавить возможность создать несколько подразделов одновременно для раздела.
- UI: Dialog с возможностью добавить несколько строк подразделов.

### 3. Копирование сметы
- Добавить действие "Копировать смету" (создаёт новую смету с теми же разделами и характеристиками, но другим номером).

### 4. Сравнение версий сметы
- На странице деталей сметы показывать изменения относительно родительской версии (если есть).
- Выделять изменённые разделы и подразделы.

### 5. Статистика по сметам
- На странице списка смет добавить карточки:
  - Всего смет
  - На сумму: {общая сумма всех смет}
  - Согласованных Заказчиком
  - В работе
  - Средняя прибыльность (%)

### 6. Печать сметы
- Добавить кнопку "Печать" на странице деталей сметы, которая открывает версию для печати (CSS @media print).

---

## Итоговый чек-лист проекта ✅

- [ ] Все страницы проектов реализованы и работают.
- [ ] Все страницы смет реализованы и работают.
- [ ] Все страницы монтажных смет реализованы и работают.
- [ ] Версионирование работает для проектов и смет.
- [ ] Замечания к проектам работают (добавление/редактирование/удаление).
- [ ] Разделы и подразделы смет работают корректно.
- [ ] Автоматические характеристики обновляются при изменении подразделов.
- [ ] Итоги смет рассчитываются и отображаются корректно.
- [ ] Создание монтажной сметы из обычной сметы работает.
- [ ] Согласование монтажной сметы работает.
- [ ] Проверки проектов работают (первичная, вторичная).
- [ ] Разрешение проектов в производство работает.
- [ ] Загрузка и скачивание файлов работает.
- [ ] Фильтрация и поиск работают на всех страницах.
- [ ] Навигация и меню обновлены.
- [ ] Все формы валидируются корректно.
- [ ] Ошибки API обрабатываются и показываются пользователю.
- [ ] Toast-уведомления работают для всех действий.
- [ ] История версий отображается корректно.

---

## Примечания по реализации

### Обработка Decimal полей
Все денежные суммы и проценты приходят с бэкенда как строки (например, "100000.00"). При отправке на бэкенд также отправлять строки. В UI можно форматировать числа для отображения, но при отправке использовать исходный формат.

### Форматирование чисел
Используй форматирование для отображения денежных сумм:
- Все денежные суммы приходят с бэкенда как строки (например, "100000.00")
- Для отображения использовать форматирование валюты (стандартное форматирование чисел как валюта)
- Формат: валюта RUB, 2 знака после запятой, локаль ru-RU

### Работа с файлами
- При загрузке файлов использовать `FormData` и `multipart/form-data`
- В FormData добавлять файл и все остальные поля формы
- При скачивании файлов использовать прямые ссылки из API или endpoint для скачивания

### Оптимистичные обновления
Для лучшего UX использовать оптимистичные обновления:
- При создании/обновлении/удалении раздела/подраздела сразу обновлять UI
- При изменении подраздела сразу обновлять характеристики (бэкенд обновит автоматически через сигнал)
- Использовать ручное обновление кэша для улучшения отзывчивости интерфейса

### Обработка ошибок
- Всегда обрабатывай ошибки API запросов
- Показывать понятные сообщения об ошибках пользователю
- Для ошибок валидации показывать ошибки под соответствующими полями формы
- Для критических ошибок (500, network error) показывать уведомления с сообщением


---

## Заключение

Этот документ содержит полное техническое задание для реализации фронтенда системы управления проектами и сметами. Все API endpoints, типы данных и UI требования описаны детально. Следуй этапам последовательно, проверяя каждый этап по чек-листу перед переходом к следующему.
