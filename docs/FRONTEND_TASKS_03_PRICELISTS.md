# Задания на разработку Фронтенда: Прайс-листы

Этот документ содержит пошаговый план создания фронтенда для системы управления прайс-листами. Каждый этап включает **полный, самодостаточный промпт** для ИИ-генератора кода. В промптах прописаны все необходимые API-контракты, типы данных и детали реализации.

**Общий стек:** Next.js (App Router), TypeScript, TailwindCSS, Shadcn UI, Axios, React Query.

**Связанные документы:**
- `FRONTEND_TASKS_01_FOUNDATION.md` — Базовые этапы (Фундамент, Справочники)
- `FRONTEND_TASKS_02_OBJECTS.md` — Объекты строительства
- `FRONTEND_TASKS_INDEX.md` — Индекс всех заданий
- `PROJECT.md` — Полная документация проекта

---

## Этап 5: Справочники (Разряды, Разделы работ, Навыки) 📚

### 🤖 Промпт для LLM
```text
Реализуем управление справочными данными для прайс-листов. Использовать библиотеку для управления состоянием и кэшированием API запросов.
**Язык интерфейса: Русский.**

**1. Страница "Разряды рабочих" (`/pricelists/worker-grades`)**
- API Endpoint: `GET /worker-grades/`, `POST /worker-grades/`, `PATCH /worker-grades/{id}/`
- Интерфейс данных:
  ```typescript
  interface WorkerGrade {
    id: number;
    grade: number; // 1-5
    name: string; // "Монтажник 1 разряда"
    default_hourly_rate: string; // Decimal string "500.00"
    is_active: boolean;
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - Таблица разрядов. Заголовки: Разряд, Название, Ставка (руб/ч), Активен.
  - Фильтр: Toggle "Показать только активные" (параметр `?is_active=true`).
  - Кнопка "Добавить разряд" открывает Dialog (Modal) с формой:
    - Разряд (Select: 1, 2, 3, 4, 5) — обязательно
    - Название (Text input) — обязательно
    - Базовая ставка (Number input, формат: 500.00) — обязательно
    - Активен (Checkbox, по умолчанию true)
  - В таблице: кнопка "Редактировать" (иконка) для каждой строки.
  - Валидация: разряд должен быть уникальным (бэкенд вернет ошибку 400).

**2. Страница "Разделы работ" (`/pricelists/work-sections`)**
- API Endpoint: `GET /work-sections/`, `POST /work-sections/`, `PATCH /work-sections/{id}/`
- Интерфейс данных:
  ```typescript
  interface WorkSection {
    id: number;
    code: string; // "VENT", "COND"
    name: string; // "Вентиляция"
    parent: number | null; // ID родительского раздела
    parent_name?: string | null; // Read-only
    is_active: boolean;
    sort_order: number;
    children?: WorkSection[]; // Вложенные разделы (read-only, при tree=true)
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - Режим отображения: Toggle "Дерево" / "Список"
    - Дерево: `GET /work-sections/?tree=true` — показывает только корневые разделы с вложенными `children`
    - Список: `GET /work-sections/` — плоский список всех разделов
  - Таблица разделов. Заголовки: Код, Название, Родительский раздел, Порядок, Активен.
  - Поиск по коду и названию (параметр `?search=...`).
  - Кнопка "Добавить раздел" открывает Dialog:
    - Код (Text input, например "VENT") — обязательно, уникальный
    - Название (Text input) — обязательно
    - Родительский раздел (Select, опционально) — показывает список активных разделов
    - Порядок сортировки (Number input, по умолчанию 0)
    - Активен (Checkbox, по умолчанию true)
  - Валидация: нельзя создать циклическую ссылку (бэкенд вернет ошибку).

**3. Страница "Навыки разрядов" (`/pricelists/worker-grade-skills`)**
- API Endpoint: `GET /worker-grade-skills/`, `POST /worker-grade-skills/`, `PATCH /worker-grade-skills/{id}/`, `DELETE /worker-grade-skills/{id}/`
- Интерфейс данных:
  ```typescript
  interface WorkerGradeSkills {
    id: number;
    grade: number; // ID разряда
    grade_detail?: {
      id: number;
      grade: number;
      name: string;
    }; // Read-only
    section: number; // ID раздела
    section_detail?: {
      id: number;
      code: string;
      name: string;
    }; // Read-only
    description: string; // Описание навыков
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - Таблица навыков. Заголовки: Разряд, Раздел, Описание.
  - Фильтры:
    - По разряду: `?grade={id}`
    - По разделу: `?section={id}`
  - Кнопка "Добавить навык" открывает Dialog:
    - Разряд (Select из списка разрядов) — обязательно
    - Раздел (Select из списка разделов) — обязательно
    - Описание (Textarea) — обязательно
  - Валидация: комбинация разряд+раздел должна быть уникальной (бэкенд вернет ошибку).
  - В таблице: кнопка "Удалить" (иконка корзины) с подтверждением.
```

### ✅ Чек-лист проверки
- [ ] Создание разряда работает, валидация уникальности работает.
- [ ] Разделы работ отображаются в режиме "Дерево" и "Список".
- [ ] Поиск по разделам работает.
- [ ] Создание навыка разряда работает, валидация уникальности работает.
- [ ] Фильтрация навыков по разряду и разделу работает.

---

## Этап 6: Справочник работ (Work Items) 🔧

### 🤖 Промпт для LLM
```text
Реализуем управление работами (Work Items) с поддержкой версионирования.
**Язык интерфейса: Русский.**

**Важно:**
- Поле `hours` опционально (может быть `null`). Если не указано, бэкенд автоматически подставляет `0`.
- Для отображения разряда использовать поле `required_grade` (может быть дробным: 2.5, 3.65), а НЕ `grade` (это ID записи).
- Поле `unit` содержит сокращенное значение (шт, м.п., м², комплект и т.д.), поле `unit_display` удалено из API.

**1. Страница "Работы" (`/pricelists/work-items`)**
- API Endpoint: `GET /work-items/`, `POST /work-items/`, `PATCH /work-items/{id}/`
- Интерфейс данных (для списка):
  ```typescript
  interface WorkItemList {
    id: number;
    article: string; // "V-001" (генерируется автоматически)
    section: number; // ID раздела
    section_name: string; // Read-only
    name: string; // Наименование работы
    unit: 'шт' | 'м.п.' | 'м²' | 'м³' | 'компл' | 'ед' | 'ч' | 'кг' | 'т' | 'точка'; // Сокращенное значение
    hours: number | null; // Опционально, может быть null (бэкенд подставит 0)
    grade: number; // ID разряда (для API, НЕ для отображения)
    grade_number: number; // Номер базового разряда (1-5)
    required_grade: number; // Точный требуемый разряд (может быть дробным: 2.5, 3.65) - использовать для отображения!
    grade_name: string; // Read-only: название разряда
    coefficient: string; // Decimal string "1.00"
    version_number: number; // Read-only
    is_current: boolean; // Read-only
  }
  ```
- Интерфейс данных (для деталей):
  ```typescript
  interface WorkItemDetail extends WorkItemList {
    section_detail: WorkSection; // Полный объект раздела
    grade_detail: WorkerGrade; // Полный объект разряда
    composition: string; // Состав работы (описание)
    comment?: string; // Комментарий к работе (опционально)
    parent_version: number | null; // ID предыдущей версии
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - Таблица работ. Заголовки: Артикул, Раздел, Наименование, Ед.изм., Часы, Разряд, Коэфф., Версия.
  - **Важно:** В колонке "Разряд" отображать `required_grade` (может быть дробным: 2.5, 3.65), а НЕ `grade` (ID).
  - В колонке "Ед.изм." отображать `unit` (сокращенное значение: шт, м.п., м² и т.д.).
  - **Фильтры:**
    - По разделу: `?section={id}` (Select)
    - По разряду: `?grade={id}` (Select)
    - Поиск: `?search=...` (по артикулу и названию)
    - Показать все версии: `?is_current=false` (Checkbox "Показать все версии")
  - По умолчанию показываются только актуальные версии (`is_current=true`).
  - Кнопка "Добавить работу" открывает Dialog:
    - Раздел (Select) — обязательно
    - Наименование (Text input) — обязательно
    - Единица измерения (Select: шт, м.п., м², м³, комплект, ед, ч, кг, т, точка) — обязательно
    - Часы (Number input, опционально) — по умолчанию пустое, минимум 0, если пустое → отправлять `null`
    - Разряд (Number input с шагом 0.01, опционально) — может быть дробным (2.5, 3.65), минимум 1, максимум 5
    - Коэффициент (Number input, формат: 1.00, по умолчанию 1.00) — обязательно, > 0
    - Состав работы (Textarea, опционально)
    - Комментарий (Textarea, опционально)
    - **Примечание:** Артикул генерируется автоматически на бэкенде (например, V-001 для раздела VENT).
  - В таблице: кнопка "Редактировать" (иконка) для каждой строки.
  - **Важно:** При редактировании работы (PATCH) бэкенд автоматически создает новую версию:
    - Старая версия: `is_current = false`
    - Новая версия: `version_number++`, `is_current = true`, `parent_version = старая_версия`
    - Артикул новой версии: `{старый_артикул}-v{номер_версии}` (например, V-001-v2)
    - После успешного редактирования показать Toast: "Работа обновлена. Создана новая версия."

**2. Страница деталей работы (`/pricelists/work-items/[id]`)**
- API Endpoint: `GET /work-items/{id}/`, `GET /work-items/{id}/versions/`
- UI:
  - Карточка работы с полной информацией.
  - Отображать `required_grade` как основной разряд (может быть дробным).
  - Отображать `unit` (сокращенное значение) в единицах измерения.
  - Секция "Комментарий" (если комментарий указан):
    - Отобразить комментарий в виде текстового блока
  - Секция "История версий":
    - Кнопка "Показать историю версий" вызывает `GET /work-items/{id}/versions/`
    - Отображает список всех версий работы (родительские и дочерние)
    - Каждая версия: Артикул, Версия, Дата создания, Актуальная
    - Клик по версии открывает детали этой версии
```

### ✅ Чек-лист проверки
- [ ] Создание работы работает, артикул генерируется автоматически.
- [ ] Поле "Часы" опционально, если пустое отправляется `null`.
- [ ] В таблице отображается `required_grade` (не `grade`), дробные разряды показываются корректно.
- [ ] В таблице отображается `unit` (сокращенное значение).
- [ ] Фильтрация по разделу, разряду и поиск работают.
- [ ] Редактирование работы создает новую версию (проверить, что старая версия `is_current=false`).
- [ ] История версий отображается корректно.
- [ ] Валидация часов (>= 0) и коэффициента (> 0) работает.
- [ ] Комментарий сохраняется и отображается корректно.

---

## Этап 7: Прайс-листы (Основной функционал) 📋

### 🤖 Промпт для LLM
```text
Реализуем основной интерфейс управления прайс-листами с полным редактированием и поддержкой дробных разрядов.
**Язык интерфейса: Русский.**

**1. Страница списка прайс-листов (`/pricelists/price-lists`)**
- API Endpoint: `GET /price-lists/`
- Интерфейс данных (для списка):
  ```typescript
  interface PriceListList {
    id: number;
    number: string; // "PL-001"
    name: string; // Название (опционально)
    date: string; // YYYY-MM-DD
    status: 'draft' | 'active' | 'archived';
    status_display: string; // Read-only: "Черновик", "Действующий", "Архивный"
    version_number: number; // Read-only
    items_count: number; // Read-only: количество работ
    agreements_count: number; // Read-only: количество согласований
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - Таблица прайс-листов. Заголовки: Номер, Название, Дата, Статус, Версия, Работ, Согласований.
  - Статусы (Badge):
    - Draft = "Черновик" (Gray)
    - Active = "Действующий" (Green)
    - Archived = "Архивный" (Gray, muted)
  - **Фильтры:**
    - По статусу: `?status=draft` (Select: Все / Черновик / Действующий / Архивный)
    - По дате: `?date_from=YYYY-MM-DD&date_to=YYYY-MM-DD` (Date pickers)
  - Поиск: `?search=...` (по номеру и названию)
  - Кнопка "Создать прайс-лист" открывает Dialog (см. ниже).
  - В таблице: клик по строке открывает детальную страницу.

**2. Форма создания прайс-листа**
- API Endpoint: `POST /price-lists/`
- Интерфейс запроса:
  ```typescript
  interface PriceListCreateRequest {
    number: string; // Обязательно
    name?: string; // Опционально
    date: string; // YYYY-MM-DD, обязательно
    status?: 'draft' | 'active' | 'archived'; // По умолчанию 'draft'
    grade_1_rate: string; // Decimal string
    grade_2_rate: string;
    grade_3_rate: string;
    grade_4_rate: string;
    grade_5_rate: string;
    work_items?: number[]; // Массив ID работ для включения (опционально)
    populate_rates?: boolean; // Заполнить ставки из справочника разрядов (по умолчанию true)
  }
  ```
- UI (Dialog/Modal):
  - Номер (Text input) — обязательно
  - Название (Text input, опционально)
  - Дата (Date picker) — обязательно
  - Статус (Select: Черновик / Действующий / Архивный, по умолчанию "Черновик")
  - **Ставки по разрядам:**
    - Checkbox "Заполнить из справочника" (по умолчанию включен)
    - Если включен: при выборе автоматически заполняются ставки из `WorkerGrade.default_hourly_rate`
    - Если выключен: поля для ввода ставок вручную (5 полей: Разряд 1-5)
  - **Работы:**
    - Multi-select или таблица с чекбоксами для выбора работ
    - Показывать только актуальные версии (`is_current=true`)
    - Можно фильтровать по разделу и разряду
    - Опционально: можно добавить работы после создания
  - Кнопки: "Отмена", "Создать"

**3. Страница деталей прайс-листа (`/pricelists/price-lists/[id]`)**
- API Endpoint: `GET /price-lists/{id}/`, `PATCH /price-lists/{id}/`
- Интерфейс данных (полный):
  ```typescript
  interface PriceListDetail {
    id: number;
    number: string;
    name: string;
    date: string;
    status: 'draft' | 'active' | 'archived';
    status_display: string;
    grade_1_rate: string;
    grade_2_rate: string;
    grade_3_rate: string;
    grade_4_rate: string;
    grade_5_rate: string;
    version_number: number;
    parent_version: number | null;
    items: PriceListItem[]; // Список позиций
    agreements: PriceListAgreement[]; // Список согласований
    items_count: number;
    total_cost: string; // Read-only: общая стоимость всех работ
    created_at: string;
    updated_at: string;
  }

  interface PriceListItem {
    id: number;
    price_list: number; // ID прайс-листа
    work_item: number; // ID работы
    work_item_detail: {
      id: number;
      article: string;
      section_name: string;
      name: string;
      unit: string; // Сокращенное значение (шт, м.п., м² и т.д.)
      hours: string;
      required_grade: number; // Точный разряд (может быть дробным)
      coefficient: string;
    }; // Read-only
    hours_override: string | null; // Переопределённые часы
    coefficient_override: string | null; // Переопределённый коэффициент
    grade_override: string | null; // Переопределённый разряд (может быть дробным, например "2.5", "3.65")
    effective_hours: string; // Read-only: hours_override || work_item.hours
    effective_coefficient: string; // Read-only: coefficient_override || work_item.coefficient
    effective_grade: string; // Read-only: grade_override || work_item.required_grade (может быть дробным)
    calculated_cost: string; // Read-only: effective_hours × effective_coefficient × ставка_разряда
    is_included: boolean; // Включена в прайс
    created_at: string;
  }

  interface PriceListAgreement {
    id: number;
    price_list: number;
    counterparty: number;
    counterparty_detail: {
      id: number;
      name: string;
      inn: string;
    }; // Read-only
    agreed_date: string; // YYYY-MM-DD
    notes: string; // Опционально
    created_at: string;
  }
  ```
- Layout страницы:
  - **Хедер:**
    - Номер прайс-листа (крупно)
    - Название (если есть)
    - Статус (Badge)
    - Дата
    - Версия
    - Кнопки действий (Dropdown menu):
      - "Редактировать" (если статус 'draft') → открывает форму редактирования:
        - Номер прайс-листа (Text input)
        - Название (Text input)
        - Дата (Date picker)
        - Статус (Select: Черновик, Действующий, Архивный)
        - Ставки по разрядам (5 Number inputs: grade_1_rate, grade_2_rate, ..., grade_5_rate)
        - API: `PATCH /price-lists/{id}/`
      - "Создать версию" (см. Этап 8)
      - "Экспорт в Excel" (см. Этап 8)
  - **Tabs (вкладки):**
    - "Позиции" (Items)
    - "Согласования" (Agreements)
    - "Информация" (Info)

**Вкладка "Позиции":**
- Таблица позиций. Заголовки: Артикул, Раздел, Наименование, Ед.изм., Часы, Разряд, Коэфф., Стоимость, Включена.
- **Важно:** В колонке "Разряд" отображать `effective_grade` (может быть дробным: 2.5, 3.65).
- **Важно:** В колонке "Ед.изм." отображать `work_item_detail.unit` (сокращенное значение).
- **Действия:**
  - Кнопка "Добавить работы" открывает Dialog:
    - Multi-select работ (только актуальные версии)
    - API: `POST /price-lists/{id}/add-items/` с телом `{ work_item_ids: [1, 2, 3] }`
  - Кнопка "Добавить позицию" открывает Dialog:
    - Работа (Select из списка работ)
    - Часы (переопределение) (Number input, опционально)
    - Коэффициент (переопределение) (Number input, опционально)
    - Разряд (переопределение) (Number input с шагом 0.01, опционально, может быть дробным: 2.5, 3.65)
    - Включена в расчёт (Checkbox, по умолчанию true)
    - API: `POST /price-list-items/`
  - Кнопка "Удалить работы" (выделить чекбоксами) → API: `POST /price-lists/{id}/remove-items/` или `DELETE /price-list-items/{id}/`
  - В каждой строке: кнопка "Редактировать" (иконка) открывает Dialog:
    - Часы (переопределение, опционально)
    - Коэффициент (переопределение, опционально)
    - Разряд (переопределение, опционально, Number input с шагом 0.01, может быть дробным: 2.5, 3.65, минимум 1, максимум 5)
    - Включена в прайс (Checkbox)
    - API: `PATCH /price-list-items/{id}/`
  - **Расчёт стоимости:**
    - Стоимость = `effective_hours × effective_coefficient × ставка_разряда`
    - Ставка берётся из прайс-листа с учётом дробного разряда (`effective_grade`)
    - Для дробных разрядов (например, 2.5) ставка вычисляется как среднее между соседними разрядами: (ставка_2 + ставка_3) / 2
    - Для дробных разрядов (например, 3.65) используется взвешенное среднее: ставка_3 × 0.35 + ставка_4 × 0.65
    - Отображать в колонке "Стоимость" (форматировать как валюта RUB с 2 знаками после запятой)
  - Внизу таблицы: **Итого:** сумма всех включенных позиций

**Вкладка "Согласования":**
- Таблица согласований. Заголовки: Контрагент, ИНН, Дата согласования, Примечания.
- Кнопка "Добавить согласование" открывает Dialog:
  - Контрагент (Select) — обязательно, только Исполнители/Поставщики (`type='vendor'` или `type='both'`)
  - Дата согласования (Date picker) — обязательно
  - Примечания (Textarea, опционально)
  - API: `POST /price-list-agreements/`
- В таблице: кнопка "Удалить" (иконка корзины) с подтверждением → API: `DELETE /price-list-agreements/{id}/`

**Вкладка "Информация":**
- Отображение всех полей прайс-листа в виде Grid (ключ-значение).
- Ставки по разрядам (таблица: Разряд 1-5, Ставка).
- Ссылка на родительскую версию (если есть).
```

### ✅ Чек-лист проверки
- [ ] Создание прайс-листа работает, ставки заполняются из справочника.
- [ ] Редактирование прайс-листа (PATCH) работает (статус, название, дата, ставки).
- [ ] Добавление работ в прайс-лист работает.
- [ ] Добавление новой позиции работает.
- [ ] Удаление работ из прайс-листа работает.
- [ ] Редактирование позиций (переопределение часов/коэффициента/разряда) работает.
- [ ] Поддержка дробного разряда для позиций работает (grade_override: 2.5, 3.65).
- [ ] Расчёт стоимости позиций работает корректно (включая дробные разряды).
- [ ] В таблице позиций отображается `effective_grade` (может быть дробным).
- [ ] В таблице позиций отображается `unit` (сокращенное значение).
- [ ] Создание согласования работает, валидация типа контрагента работает.
- [ ] Удаление согласования работает.

---

## Этап 8: Версионирование и экспорт 📄

### 🤖 Промпт для LLM
```text
Реализуем создание версий прайс-листов и экспорт в Excel.
**Язык интерфейса: Русский.**

**1. Создание версии прайс-листа**
- API Endpoint: `POST /price-lists/{id}/create-version/`
- Логика на бэкенде:
  - Текущая версия: `status = 'archived'`
  - Создаётся копия со всеми ставками и позициями
  - Новая версия: `version_number++`, `parent_version = текущая_версия`, `status = 'draft'`
  - Номер новой версии: `{старый_номер}-v{номер_версии}` (например, PL-001-v2)
- UI:
  - В детальной странице прайс-листа, в меню действий: пункт "Создать версию"
  - При клике открывается Dialog (Modal):
    - Информационное сообщение:
      - "Новая версия наследует все данные (ставки и позиции)"
      - "Текущая версия будет переведена в статус 'Архивный'"
      - "Новая версия получит статус 'Черновик'"
    - Кнопки: "Отмена", "Создать версию"
  - После успешного создания:
    - Показать Toast: "Версия успешно создана"
    - Перенаправить на страницу новой версии (или обновить список)
    - Новая версия имеет статус "Черновик"

**2. Экспорт прайс-листа в Excel**
- API Endpoint: `GET /price-lists/{id}/export/`
- Response: Excel файл (Content-Type: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`)
- UI:
  - В детальной странице прайс-листа, в меню действий: пункт "Экспорт в Excel"
  - При клике:
    - Показать Toast: "Экспорт начат..."
    - Выполнить GET запрос на endpoint с `responseType: 'blob'`
    - Обработать ответ как blob и скачать файл
    - Имя файла: `pricelist_{number}_{date}.xlsx`
    - Показать Toast: "Файл успешно скачан"
- Структура Excel-файла (на бэкенде):
  - Заголовок: "Прайс-лист №{number} от {date}"
  - Ставки по разрядам (таблица)
  - Таблица работ: Артикул, Раздел, Наименование, Ед.изм., Часы, Разряд, Коэфф., Стоимость, Комментарий
  - Итого (сумма всех включенных работ)
```

### ✅ Чек-лист проверки
- [ ] Создание версии прайс-листа работает, старая версия архивируется.
- [ ] Новая версия наследует все позиции и ставки.
- [ ] Экспорт в Excel работает, файл скачивается корректно.
- [ ] Имя файла при экспорте содержит номер и дату прайс-листа.

---

## Этап 9: Интеграция в меню и навигация 🧭

### 🤖 Промпт для LLM
```text
Добавить раздел "Прайс-листы" в основное меню приложения.
**Язык интерфейса: Русский.**

**1. Обновление Sidebar (Левое меню)**
- Добавить новый пункт меню: "💰 Прайс-листы" (`/pricelists`)
- Подменю (при клике или hover):
  - "📋 Прайс-листы" (`/pricelists/price-lists`)
  - "🔧 Работы" (`/pricelists/work-items`)
  - "📚 Разделы работ" (`/pricelists/work-sections`)
  - "⭐ Разряды" (`/pricelists/worker-grades`)
  - "🎓 Навыки разрядов" (`/pricelists/worker-grade-skills`)

**2. Breadcrumbs**
- На всех страницах прайс-листов показывать breadcrumbs:
  - Главная → Прайс-листы → [Текущая страница]
  - Например: Главная → Прайс-листы → Прайс-листы → PL-001

**3. Навигация между связанными сущностями**
- В таблице работ: клик по разделу открывает страницу разделов с фильтром по этому разделу
- В детальной странице работы: ссылка на раздел и разряд (открывают соответствующие страницы)
- В детальной странице прайс-листа: ссылки на работы в позициях (открывают детали работы)
```

### ✅ Чек-лист проверки
- [ ] Раздел "Прайс-листы" добавлен в меню.
- [ ] Навигация между страницами работает.
- [ ] Breadcrumbs отображаются корректно.
- [ ] Ссылки на связанные сущности работают.

---

## Итоговый чек-лист проекта ✅

- [ ] Все справочники (разряды, разделы, навыки) реализованы и работают.
- [ ] Справочник работ реализован, версионирование работает.
- [ ] Поле "Часы" опционально, корректно обрабатывается.
- [ ] Отображение разрядов использует `required_grade` (дробные разряды работают).
- [ ] Отображение единиц измерения использует `unit` (сокращенные значения).
- [ ] Поле "Комментарий" для работ реализовано и работает.
- [ ] Создание и редактирование прайс-листов работает.
- [ ] Добавление/удаление работ в прайс-лист работает.
- [ ] Редактирование позиций (переопределения) работает.
- [ ] Поддержка дробного разряда для позиций работает (grade_override).
- [ ] Расчёт стоимости позиций работает корректно (включая дробные разряды).
- [ ] Согласование прайс-листов работает.
- [ ] Создание версий прайс-листов работает.
- [ ] Экспорт в Excel работает.
- [ ] Навигация и меню обновлены.
- [ ] Все формы валидируются корректно.
- [ ] Ошибки API обрабатываются и показываются пользователю.
- [ ] Toast-уведомления работают для всех действий.

---

## Дополнительные улучшения (опционально) ✨

### 1. Массовое добавление работ в прайс-лист
- Добавить возможность выбрать несколько работ из справочника и добавить их в прайс-лист одной операцией.
- UI: Dialog с поиском и фильтрами, таблица с чекбоксами, кнопка "Добавить выбранные".

### 2. Копирование прайс-листа
- Добавить действие "Копировать прайс-лист" (создаёт новый прайс-лист с теми же позициями, но другим номером и датой).

### 3. Сравнение версий прайс-листа
- На странице деталей прайс-листа показывать изменения относительно родительской версии (если есть).

### 4. Печать прайс-листа
- Добавить кнопку "Печать" на странице деталей, которая открывает версию для печати (CSS @media print).

### 5. Статистика по прайс-листам
- На странице списка прайс-листов добавить карточки:
  - Всего прайс-листов
  - Действующих
  - Черновиков
  - Общая стоимость всех прайс-листов
