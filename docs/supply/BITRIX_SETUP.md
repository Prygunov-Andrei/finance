# Подключение Bitrix24 к ERP

## Обзор

Интеграция с Bitrix24 позволяет автоматически передавать запросы на снабжение и счета из CRM-канбана в ERP для обработки и оплаты.

**Схема работы:**
```
Bitrix24 CRM → Outgoing Webhook → ERP API → Celery задачи → Invoice
```

---

## Предварительные требования

1. Аккаунт администратора Bitrix24
2. Развёрнутый ERP-сервер с доступным HTTPS-адресом
3. Настроенный Celery worker и Redis

---

## Шаг 1: Создание Outgoing Webhook в Bitrix24

### 1.1. Перейдите в настройки Bitrix24

1. Откройте ваш портал Bitrix24 (например, `https://your-company.bitrix24.ru`)
2. Перейдите в **Разработчикам** → **Другое** → **Исходящий вебхук**
3. Нажмите **"Добавить"**

### 1.2. Настройте Outgoing Webhook

| Параметр | Значение |
|----------|----------|
| **URL обработчика** | `https://your-erp-domain.ru/api/v1/supply/webhook/bitrix/` |
| **Тип события** | `ONCRMDEALLUPDATE` (Обновление сделки) |
| **Код авторизации** | Скопируйте — это будет `outgoing_webhook_token` |

> ⚠️ URL должен быть доступен из интернета (HTTPS обязателен)

### 1.3. Скопируйте учётные данные

После создания вебхука Bitrix24 покажет:
- **URL вебхука** (Inbound webhook URL) — для вызовов API Bitrix24 из ERP
- **Код авторизации** (Auth token) — для проверки входящих webhook-запросов

---

## Шаг 2: Создание Inbound Webhook (для доступа к API Bitrix24)

ERP необходим доступ к API Bitrix24 для получения данных сделок, комментариев и файлов.

### 2.1. Создайте входящий вебхук

1. Перейдите в **Разработчикам** → **Другое** → **Входящий вебхук**
2. Нажмите **"Добавить"**

### 2.2. Настройте права доступа

Включите следующие права:

| Право | Описание |
|-------|----------|
| `crm` | Доступ к CRM (сделки, контакты) |
| `disk` | Доступ к файлам |
| `task` | Доступ к задачам (если используется) |

### 2.3. Скопируйте Webhook URL

Формат: `https://your-company.bitrix24.ru/rest/{user_id}/{secret}/`

Это значение будет использоваться как `webhook_url` в настройках ERP.

---

## Шаг 3: Настройка интеграции в ERP

### 3.1. Через интерфейс ERP

1. Войдите в ERP с правами администратора
2. Перейдите в **Настройки** → **Битрикс24**
3. Нажмите **"Добавить интеграцию"**

### 3.2. Заполните параметры

| Поле | Описание | Пример |
|------|----------|--------|
| **Название** | Произвольное название | "Основная воронка снабжения" |
| **URL портала** | Адрес Bitrix24 | `https://your-company.bitrix24.ru` |
| **Webhook URL** | Inbound Webhook (Шаг 2) | `https://your-company.bitrix24.ru/rest/1/abc123/` |
| **Токен Outgoing Webhook** | Код из Шага 1 | `xyztoken123` |
| **ID воронки (категория)** | ID воронки CRM | `0` (основная) |
| **ID стадии** | Стадия "Передан в Оплату" | `WON` или `C1:NEW` |
| **Маппинг поля "Договор"** | Пользовательское поле или `TITLE` | `UF_CRM_CONTRACT` |
| **Маппинг поля "Объект"** | Пользовательское поле или `TITLE` | `UF_CRM_OBJECT` |

### 3.3. Определение ID стадии канбана

Чтобы узнать ID нужной стадии:

```bash
# Через API Bitrix24:
curl "https://your-company.bitrix24.ru/rest/{user_id}/{secret}/crm.dealcategory.stage.list?id=0"
```

Или в интерфейсе Bitrix24: **CRM** → **Настройки** → **Справочники** → **Стадии сделок**

---

## Шаг 4: Настройка пользовательских полей (опционально)

Для точного маппинга рекомендуем создать пользовательские поля в Bitrix24:

### 4.1. Создание поля "Договор"

1. **CRM** → **Настройки** → **Пользовательские поля** → **Сделки**
2. Добавьте поле типа **"Строка"**
3. Название: `Номер договора`
4. Код: `UF_CRM_CONTRACT`

### 4.2. Создание поля "Объект"

1. Аналогично создайте поле **"Строка"**
2. Название: `Объект`
3. Код: `UF_CRM_OBJECT`

> 💡 Если пользовательские поля не настроены, ERP попытается извлечь данные из заголовка сделки по шаблону: `{номер_договора} {название_объекта} — {описание}`

---

## Шаг 5: Настройка структуры карточки канбана

### Рекомендуемый формат заголовка сделки

```
{номер_договора} {объект} — {описание}. {Поставщик} (сч.{номер_счёта})
```

**Пример:** `115 Озёры ЖК-расходка (диски). Все Инструменты (сч.2602-421014-33318)`

### Правила прикрепления файлов в комментариях

| Тип | Правило | Пример |
|-----|---------|--------|
| **Запрос** | Комментарий содержит слово "запрос" | "Запрос: нужны диски отрезные 125 — 20 шт" |
| **Счёт** | Комментарий НЕ содержит слово "запрос" | "Счёт от Все Инструменты" |

> Запрос может быть текстовым (без файла) — например, устный запрос от монтажника

---

## Шаг 6: Проверка интеграции

### 6.1. Тест webhook

1. Создайте тестовую сделку в Bitrix24
2. Переместите её в стадию "Передан в Оплату"
3. Проверьте в ERP: **Снабжение** → **Запросы из Битрикс**
4. Должен появиться новый запрос со статусом "Получен"

### 6.2. Проверка логов

```bash
# Логи Celery worker
docker logs -f celery_worker

# Или напрямую
cd backend && celery -A finans_assistant worker -l debug
```

### 6.3. Частые проблемы

| Проблема | Решение |
|----------|---------|
| Webhook не приходит | Проверьте HTTPS-доступность URL, настройки Outgoing Webhook |
| Токен не совпадает | Убедитесь, что `outgoing_webhook_token` в ERP совпадает с кодом в Bitrix24 |
| Файлы не скачиваются | Проверьте права `disk` в Inbound Webhook |
| Объект не определяется | Настройте пользовательские поля или проверьте формат заголовка |

---

## Переменные окружения

```bash
# .env файл бекенда
BITRIX_WEBHOOK_TIMEOUT=30  # Таймаут запросов к API Bitrix24 (секунды)
```

---

## Безопасность

1. **HTTPS обязателен** — webhook URL должен использовать HTTPS
2. **Токен проверяется** — каждый входящий webhook проверяется по `outgoing_webhook_token`
3. **Webhook URL уникален** — не публикуйте его
4. **Минимальные права** — запрашивайте только необходимые права (crm, disk)

---

*Последнее обновление: Февраль 2026*
