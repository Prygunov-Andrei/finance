# Workflow снабжения: от запроса до оплаты

## Общая схема

```
┌─────────────┐     ┌──────────┐     ┌──────────────┐     ┌─────────────┐
│  Bitrix24   │────▶│   ERP    │────▶│  Оператор-   │────▶│  Директор-  │
│  CRM Канбан │     │  Webhook │     │  Снабженец   │     │  Контролёр  │
└─────────────┘     └──────────┘     └──────────────┘     └─────────────┘
                                                                 │
                                                                 ▼
                                                          ┌─────────────┐
                                                          │    Банк     │
                                                          │  (Точка)    │
                                                          └─────────────┘
```

---

## Этапы

### 1. Создание запроса (Bitrix24)

**Участник:** Оператор-Снабженец (в Bitrix24)

1. Оператор создаёт карточку в канбане Bitrix24
2. Заполняет заголовок: `{договор} {объект} — {описание}. {Поставщик} (сч.{номер})`
3. Прикрепляет файл запроса (комментарий со словом "запрос")
4. Прикрепляет PDF счёта (комментарий без слова "запрос")
5. Перемещает карточку в стадию **"Передан в Оплату"**

### 2. Приём данных (ERP, автоматически)

**Участник:** Система (Celery)

1. Bitrix24 отправляет Outgoing Webhook в ERP
2. ERP проверяет токен, создаёт `SupplyRequest` со статусом `received`
3. Celery-задача `process_bitrix_deal`:
   - Получает данные сделки через API Bitrix24
   - Парсит заголовок → определяет Объект и Договор
   - Скачивает файлы из комментариев
   - Определяет тип файла (запрос/счёт) по ключевым словам
   - Создаёт `Invoice` для каждого счёта со статусом `recognition`

### 3. Распознавание LLM (автоматически)

**Участник:** Система (Celery + LLM)

1. Celery-задача `recognize_invoice`:
   - Отправляет PDF счёта в LLM (OpenAI/Gemini)
   - Получает: номер счёта, дату, контрагента, позиции (товары/услуги)
   - Сопоставляет позиции с каталогом через `ProductMatcher`:
     - Шаг 1: Fuzzy matching (fuzzywuzzy) по названию и алиасам
     - Шаг 2: LLM-сравнение для неоднозначных совпадений
   - Создаёт `InvoiceItem` для каждой позиции
   - Переводит Invoice в статус `review`
   - Отправляет уведомление Оператору-Снабженцу

### 4. Проверка оператором (ERP)

**Участник:** Оператор-Снабженец

**Страница:** Снабжение → Счета на оплату

1. Оператор видит список счетов со статусом `review`
2. Проверяет корректность распознавания:
   - Номер счёта, дата, сумма
   - Контрагент
   - Позиции: верно ли сопоставлены с каталогом?
3. Исправляет ошибки сопоставления товаров:
   - Если товар новый → создаётся в каталоге
   - Если дубликат → объединяет через алиас
4. После проверки нажимает **"В реестр"** → статус `in_registry`

### 5. Одобрение директором (ERP)

**Участник:** Директор-контролёр

**Страница:** Снабжение → Дашборд

1. Директор видит:
   - Остатки на всех счетах
   - Общую сумму счетов в реестре
   - Счета, сгруппированные по объектам и категориям
   - Счета с подгорающими сроками оплаты
2. Принимает решение по каждому счёту:
   - ✅ **Одобрить** → статус `approved`
   - ❌ **Отклонить** (с комментарием) → статус `review` (возврат оператору)
   - 📅 **Перенести** (новая дата + комментарий) → остаётся в `in_registry`

### 6. Отправка в банк (автоматически)

**Участник:** Система

1. После одобрения:
   - Создаётся `BankPaymentOrder` (черновик или сразу отправка)
   - Invoice переводится в статус `sending`
2. Платёжное поручение отправляется в банк через API Точка
3. Банк подтверждает исполнение → статус `paid`

---

## Жизненный цикл Invoice (Счёт на оплату)

```
recognition → review → in_registry → approved → sending → paid
                ↑          │                        
                └──────────┘ (reject → возврат на review)
                
В любой момент: → cancelled
```

| Статус | Описание | Кто переводит |
|--------|----------|---------------|
| `recognition` | LLM распознаёт PDF | Система |
| `review` | Ожидает проверки оператором | Система / Директор (при отклонении) |
| `in_registry` | В реестре, ожидает одобрения | Оператор |
| `approved` | Одобрен к оплате | Директор |
| `sending` | Отправлен в банк | Система |
| `paid` | Оплачен | Система (подтверждение банка) |
| `cancelled` | Отменён | Оператор / Директор |

---

## Периодические платежи

**Участник:** Линейный бухгалтер

### Настройка

1. Перейти в **Снабжение** → **Периодические платежи**
2. Создать запись:
   - Контрагент (например, арендодатель)
   - Категория расходов
   - Сумма (фиксированная или ориентировочная)
   - Периодичность: ежемесячно / ежеквартально / ежегодно
   - День месяца для генерации

### Автогенерация

- Celery Beat ежедневно (в 06:00) проверяет `RecurringPayment`
- Если наступила дата генерации → создаётся Invoice со статусом `in_registry`
- Invoice сразу появляется в реестре для одобрения директором

---

## Учёт доходов

**Участник:** Линейный бухгалтер

1. Перейти в **Снабжение** → **Доходы**
2. Создать запись о доходе:
   - Счёт зачисления
   - Сумма, дата
   - Контрагент, договор (опционально)
   - Скан документа (опционально)

---

*Последнее обновление: Февраль 2026*
