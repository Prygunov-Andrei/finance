# Планирование моделей и их связей

## Финансовая автоматизация строительной компании

### Концепция (Слой 1)
- **Объект**: Физическое место строительства (уникальное название, адрес)
- **Договор**: Связь с объектом, может быть несколько договоров на один объект
- **Платежи**: Расходы и доходы по договорам с привязкой к хранилищу документов
- **Реестр**: Планируемые платежи и задолженности
- **Cash-flow**: Расчетный показатель за период
- **Импорт данных**: Регламентируемые загрузки табличных файлов и документов

### Основная модель: Объект

#### Текущие поля модели Object:
- `name` (CharField, max_length=200, unique=True) — Название объекта
- `address` (CharField, max_length=255) — Адрес объекта
- `description` (TextField, blank=True) — Описание объекта
- `created_at` (DateTimeField, auto_now_add=True) — Дата создания
- `updated_at` (DateTimeField, auto_now=True) — Дата обновления

#### Модель Contract (реализована):
- `object` (ForeignKey) — ссылка на объект
- `number` (CharField, max_length=100) — уникален в пределах объекта
- `name` (CharField, max_length=255)
- `contract_date`, `start_date`, `end_date` (DateField)
- `contractor` (CharField, max_length=255)
- `total_amount` (DecimalField, max_digits=14, decimal_places=2)
- `currency` (CharField, choices=RUB/USD/EUR)
- `vat_rate` (DecimalField, default=20.00)
- `vat_included` (BooleanField)
- `status` (CharField, choices)
- `document_link` (CharField, max_length=500, blank=True)
- `notes` (TextField, blank=True)
- `created_at`, `updated_at`

#### Модель Payment (реализована):
- `contract` (ForeignKey) — ссылка на договор
- `payment_type` (CharField, choices=expense/income) — тип платежа
- `payment_date` (DateField) — дата платежа
- `amount` (DecimalField, max_digits=14, decimal_places=2) — сумма
- `company_account` (CharField, max_length=100) — счёт компании
- `description` (TextField) — назначение платежа
- `document_link` (CharField, max_length=500) — ссылка на документ
- `import_batch_id` (CharField, max_length=100) — идентификатор импорта
- `created_at`, `updated_at`

#### Модель PaymentRegistry (реализована):
- `contract` (ForeignKey) — ссылка на договор
- `planned_date` (DateField) — плановая дата
- `amount` (DecimalField, max_digits=14, decimal_places=2) — сумма
- `status` (CharField, choices=planned/approved/cancelled) — статус
- `initiator` (CharField, max_length=255) — инициатор платежа
- `comment` (TextField) — комментарий
- `created_at`, `updated_at`

#### Модель ImportLog (реализована):
- `import_batch_id` (CharField, unique) — уникальный идентификатор импорта
- `user` (ForeignKey, nullable) — пользователь, выполнивший импорт
- `file_name` (CharField) — имя файла
- `file_type` (CharField, choices) — тип файла: платежи/планы/поступления/задолженность
- `file_size` (PositiveIntegerField) — размер файла
- `file_path` (CharField) — путь к файлу
- `status` (CharField, choices) — статус обработки
- `records_count`, `success_count`, `error_count` (PositiveIntegerField) — статистика
- `errors` (TextField) — описание ошибок
- `import_date` (DateTimeField) — дата импорта
- `created_at`, `updated_at`

#### Реализованные функции импорта:
- ✅ Утилиты для работы с журналом импорта (`imports/utils.py`)
- ✅ Валидаторы данных (`imports/validators.py`)
- ✅ Базовый класс парсера (`imports/parsers.py`)

#### Планируемые функции (на будущее):
- [ ] Конкретные парсеры для Excel/CSV файлов (когда появится необходимость в импорте)
- [ ] REST API endpoint для загрузки файлов
- [ ] Обработка файлов в фоновых задачах

**Примечание**: Базовая структура для импорта готова (BaseParser, валидаторы, утилиты). Конкретные парсеры будут реализованы позже, когда появится реальная потребность в работе с файлами.

#### Приоритеты разработки:
1. ✅ Базовая модель Object + поле `address`
2. ✅ Модель Contract (договоры)
3. ✅ Модель Payment (платежи)
4. ✅ Модель PaymentRegistry (реестр)
5. ✅ Модель ImportLog
6. 🔄 Процессы импорта данных