# Задания на разработку Фронтенда (Next.js)

Этот документ содержит пошаговый план создания фронтенда. Каждый этап включает **полный, самодостаточный промпт** для ИИ-генератора кода. В промптах прописаны все необходимые API-контракты, типы данных и детали реализации, чтобы модели не требовался доступ к файлам бэкенда.

**Общий стек:** Next.js (App Router), TypeScript, TailwindCSS, Shadcn UI, Axios, React Query.

**Связанные документы:**
- `FRONTEND_TASKS_02_OBJECTS.md` — Объекты строительства
- `FRONTEND_TASKS_03_PRICELISTS.md` — Прайс-листы
- `FRONTEND_TASKS_04_PROJECTS_ESTIMATES.md` — Проекты и Сметы
- `FRONTEND_TASKS_05_TKP_MP.md` — ТКП и МП
- `FRONTEND_TASKS_06_CONTRACTS.md` — Договоры
- `FRONTEND_TASKS_07_PAYMENTS.md` — Платежи
- `FRONTEND_TASKS_08_COMMUNICATIONS.md` — Переписка
- `FRONTEND_TASKS_09_DASHBOARD.md` — Dashboard и Аналитика
- `FRONTEND_TASKS_10_MISSING_ITEMS.md` — Исправление упущений
- `FRONTEND_TASKS_INDEX.md` — Индекс всех заданий

---

## Этап 1: Фундамент (Setup, Auth, Layout) 🏗️

### 🤖 Промпт для LLM
```text
Ты — Senior Frontend Developer. Твоя задача — создать основу для web-приложения "Finance Assistant" с нуля.

**Технический стек:**
- Framework: Next.js 14+ (App Router).
- Language: TypeScript.
- Styling: TailwindCSS.
- UI Kit: Shadcn UI (установи компоненты: Button, Input, Card, Form, Label, Toast, Sheet/Sidebar, Avatar, DropdownMenu).
- State Management: React Query (TanStack Query v5).
- HTTP Client: Axios.
- Icons: Lucide React.

**Задание 1: Инициализация и API Клиент**
1. Развернуть проект.
2. Настроить HTTP клиент:
   - Base URL: использовать переменную окружения `NEXT_PUBLIC_API_URL` (по умолчанию `https://finance.ngrok.app/api/v1`).
   - Request Interceptor: Добавлять заголовок `Authorization: Bearer <token>` если токен есть в LocalStorage/Cookies.
   - Response Interceptor: Если приходит 401 Unauthorized -> очищать токены и перенаправлять на `/login`.
3. В файле `.env.local` (или `.env`) установить переменную:
   ```
   NEXT_PUBLIC_API_URL=https://finance.ngrok.app/api/v1
   ```
   **Важно:** Домен `finance.ngrok.app` уже добавлен в `ALLOWED_HOSTS` и `CSRF_TRUSTED_ORIGINS` на бэкенде.

**Задание 2: Авторизация**
1. Создать страницу `/login`.
2. Реализовать форму входа (username, password).
3. Логика:
   - Отправлять POST на `/auth/jwt/create/`
   - Тело запроса: `{ "username": "...", "password": "..." }`
   - Ответ успешный (200): `{ "access": "ey...", "refresh": "ey..." }`
   - Сохранить токены.
   - Перенаправить пользователя на `/dashboard`.
   - Показывать уведомления об ошибках входа.

**Задание 3: Layout приложения**
1. Создать `DashboardLayout`, который оборачивает все страницы кроме `/login`.
2. **Sidebar (Левое меню):**
   - Логотип/Название вверху.
   - Ссылки на разделы:
     - 🏠 Dashboard (`/dashboard`)
     - 👥 Контрагенты (`/counterparties`)
     - 🏗️ Объекты (`/objects`)
     - 💰 Прайс-листы (`/pricelists`) — подменю: Прайс-листы, Работы, Разделы, Разряды
     - 📁 Проекты и Сметы (`/estimates`) — подменю: Проекты, Сметы, Монтажные сметы
     - 📋 ТКП и МП (`/proposals`) — подменю: ТКП, МП, Справочники
     - 📄 Договоры (`/contracts`) — подменю: Договоры, Рамочные договоры, Акты
     - 💸 Платежи (`/payments`) — подменю: Платежи, Реестр платежей
     - 📧 Переписка (`/communications`)
     - ⚙️ Настройки (`/settings`) — подменю: Мои компании, Счета
3. **Header (Верхняя панель):**
   - Breadcrumbs (автоматические на основе пути).
   - User Menu (справа): Аватар с инициалами. При клике -> Dropdown с кнопкой "Logout".
4. Реализовать Protected Route: если нет токена, любой переход внутри `/dashboard` редиректит на `/login`.

**Задание 4: Заглушка Dashboard**
- На странице `/dashboard` выведи простой текст: "Welcome to Finance Assistant".
```

### ✅ Чек-лист проверки
- [ ] Проект запускается, стили Tailwind работают.
- [ ] Страница входа работает, токены сохраняются.
- [ ] Sidebar отображает все пункты меню.
- [ ] Logout работает корректно.
- [ ] Неавторизованный доступ к `/dashboard` закрыт.
- [ ] API URL настроен на `https://finance.ngrok.app/api/v1`.

---

## Этап 2: Справочники (Settings & Counterparties) 📚

### 🤖 Промпт для LLM
```text
Реализуем управление справочными данными. Использовать библиотеку для управления состоянием и кэшированием API запросов.

**1. Страница "Settings" (`/settings`)**
**Язык интерфейса: Русский.**
Использовать компонент Tabs для переключения между "Мои компании" (Legal Entities), "Счета" (Accounts), "Категории расходов" (Expense Categories) и "Налоговые системы" (Tax Systems).

**Вкладка: Налоговые системы (Tax Systems)**
- API Endpoint: `GET /tax-systems/` (read-only справочник)
- Интерфейс данных:
  ```typescript
  interface TaxSystem {
    id: number;
    code: string; // "osn_vat_20", "usn_income", etc. (unique)
    name: string; // "ОСН с НДС 20%", "УСН Доходы", etc.
    vat_rate?: string; // Decimal string "20.00" или null
    has_vat: boolean; // Есть ли НДС
    description?: string; // Описание
    is_active: boolean;
  }
  ```
- UI:
  - Таблица налоговых систем. Заголовки: Код, Название, Ставка НДС, Есть НДС, Активна.
  - Только для чтения (справочник, заполняется на бэкенде).
  - Используется в форме создания/редактирования LegalEntity для выбора системы налогообложения.

**Вкладка: Мои компании (Legal Entities)**
- API Endpoint: `GET /legal-entities/`, `POST /legal-entities/`, `PATCH /legal-entities/{id}/`
- Интерфейс данных:
  ```typescript
  interface LegalEntity {
    id: number;
    name: string; // Название (ООО "Ромашка")
    short_name?: string; // Краткое название
    inn: string;  // ИНН (unique)
    kpp?: string; // КПП
    ogrn?: string; // ОГРН
    tax_system: number; // ID системы налогообложения
    tax_system_name?: string; // Read-only: название системы
    director?: number; // ID пользователя (генеральный директор)
    director_name?: string; // Read-only: ФИО директора
    director_position?: string; // Read-only: должность директора
    is_active: boolean;
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - Список карточек или таблица. Колонки: Название, ИНН, Система налогообложения, Директор, Активна.
  - Кнопка "Добавить компанию" открывает Dialog:
    - Название* (Text input) — обязательно
    - Краткое название (Text input, опционально)
    - ИНН* (Text input) — обязательно, unique
    - КПП (Text input, опционально)
    - ОГРН (Text input, опционально)
    - Система налогообложения* (Select из GET /tax-systems/) — обязательно
    - Генеральный директор (Select из списка пользователей, опционально)
    - ФИО директора (Text input, опционально) — для документов
    - Должность директора (Text input, опционально, по умолчанию "Генеральный директор")
    - Активна (Checkbox, по умолчанию true)
  - В таблице: кнопка "Редактировать" для каждой компании.
  - **Важно:** Поля director, director_name, director_position используются для подстановки подписанта в ТКП.

**Вкладка: Счета (Accounts)**
- API Endpoint: `GET /accounts/`, `POST /accounts/`, `PATCH /accounts/{id}/`
- Интерфейс данных:
  ```typescript
  interface Account {
    id: number;
    legal_entity: number; // ID юрлица
    legal_entity_name: string; // Read-only
    name: string; // Название счета
    number: string; // Номер счета (unique в рамках юрлица)
    account_type: 'bank_account' | 'cash' | 'deposit' | 'currency_account';
    bank_name?: string;
    bik?: string;
    currency: 'RUB' | 'USD' | 'EUR';
    current_balance: string; // Приходит строкой ("1000.00")!
    initial_balance?: string; // Decimal string
    balance_date?: string; // YYYY-MM-DD
    location?: string; // Для касс
    description?: string;
    is_active: boolean;
    created_at: string;
    updated_at: string;
  }
  ```
- UI:
  - Таблица. Колонки: Юрлицо, Название, Тип, Банк, Номер, Валюта, Баланс, Активен.
  - Типы (Badge): bank_account="Расчётный счёт", cash="Касса", deposit="Депозит", currency_account="Валютный счёт".
  - **Важно:** `current_balance` приходит строкой (например, `"1000.00"`). Для отображения необходимо преобразовать строку в число и отформатировать как валюту RUB.
  - Кнопка "Добавить счет" открывает Dialog:
    - Юрлицо* (Select из GET /legal-entities/) — обязательно
    - Название* (Text input) — обязательно
    - Номер* (Text input) — обязательно, уникален в рамках юрлица
    - Тип счёта* (Select: Расчётный счёт / Касса / Депозит / Валютный счёт) — обязательно
    - Банк (Text input, показывается только для bank_account)
    - БИК (Text input, показывается только для bank_account)
    - Валюта* (Select: RUB / USD / EUR) — обязательно
    - Начальный остаток (Number input, Decimal)
    - Дата начального остатка (Date picker, показывается если указан начальный остаток)
    - Местоположение (Text input, показывается только для cash)
    - Описание (Textarea)
    - Активен (Checkbox, по умолчанию true)
  - В таблице: клик по строке открывает детальную страницу счёта.

**Детальная страница счёта (`/settings/accounts/[id]`)**
- API: `GET /accounts/{id}/`, `GET /accounts/{id}/balance/`
- Структура страницы:
  - **Шапка:** Название счёта, номер, тип, валюта
  - **Панель информации:** Юрлицо, банк, БИК, текущий баланс
  - **Вкладка "История остатков":**
    - API: `GET /accounts/{id}/balance/`
    - Интерфейс данных:
      ```typescript
      interface AccountBalance {
        id: number;
        account: number; // ID счёта
        balance_date: string; // YYYY-MM-DD
        balance: string; // Decimal string
      }
      ```
    - Таблица остатков: Дата, Остаток
    - График изменения остатка (опционально, использовать библиотеку для графиков)
    - Сортировка по дате (новые сверху)

**Вкладка: Категории расходов (Expense Categories)**
- API Endpoint: `GET /expense-categories/`, `POST /expense-categories/`, `PATCH /expense-categories/{id}/`
- Интерфейс данных:
  ```typescript
  interface ExpenseCategory {
    id: number;
    name: string; // Название категории
    code?: string; // Код категории
    parent?: number; // ID родительской категории
    parent_name?: string; // Read-only
    requires_contract: boolean; // Требует ли привязки к договору
    is_active: boolean;
    sort_order: number;
    children?: ExpenseCategory[]; // Вложенные категории (read-only, при tree=true)
  }
  ```
- UI:
  - Режим отображения: Toggle "Дерево" / "Список"
  - Таблица категорий. Заголовки: Название, Код, Родительская категория, Требует договор, Активна, Порядок.
  - Кнопка "Добавить категорию" открывает Dialog:
    - Название* (Text input) — обязательно
    - Код (Text input, опционально)
    - Родительская категория (Select, опционально) — показывает список активных категорий
    - Требует договор (Checkbox, по умолчанию false)
    - Порядок сортировки (Number input, по умолчанию 0)
    - Активна (Checkbox, по умолчанию true)
  - В таблице: кнопка "Редактировать" для каждой категории.
  - **Важно:** Категории используются при создании платежей и заявок в реестре.

**2. Страница "Контрагенты" (`/counterparties`)**
**Язык интерфейса: Русский.**
- API Endpoint: `GET /counterparties/`, `POST /counterparties/`, `PATCH /counterparties/{id}/`
- Интерфейс данных:
  ```typescript
  interface Counterparty {
    id: number;
    name: string;
    short_name?: string;
    inn: string;
    kpp?: string;
    ogrn?: string;
    type: 'customer' | 'vendor' | 'both'; // Тип контрагента
    vendor_subtype?: 'supplier' | 'executor' | 'both' | null; // Подтип (только для type='vendor' или 'both')
    vendor_subtype_display?: string; // Read-only: "Поставщик" / "Исполнитель" / "Исполнитель и Поставщик"
    legal_form: 'ooo' | 'ip' | 'self_employed' | 'fiz'; // Обязательно
    contact_info?: string;
    is_active: boolean;
    created_at: string;
  }
  ```
- UI:
  - **Фильтр (Tabs):** Все / Заказчики / Поставщики / Исполнители
    - API для фильтрации:
      - `GET /counterparties/?type=customer` — заказчики
      - `GET /counterparties/?type=vendor&vendor_subtype=supplier` — поставщики
      - `GET /counterparties/?type=vendor&vendor_subtype=executor` — исполнители
      - `GET /counterparties/?type=vendor&vendor_subtype=both` — и поставщики, и исполнители
  - Таблица контрагентов. Заголовки: Название, ИНН, Тип, Подтип, Правовая форма, Контакты.
    - Колонка "Подтип" отображается только для строк, где `type='vendor'` или `type='both'`:
      - Если `vendor_subtype === 'supplier'` → показать "Поставщик"
      - Если `vendor_subtype === 'executor'` → показать "Исполнитель"
      - Если `vendor_subtype === 'both'` → показать "Исполнитель и Поставщик"
      - Если `vendor_subtype === null` → показать "—" (для старых записей)
  - Кнопка "Добавить контрагента" открывает Dialog (Modal).
  - **Форма создания/редактирования:**
    - Название* (Text input) — обязательно
    - Краткое название (Text input, опционально)
    - Правовая форма* (Select: ООО="ooo", ИП="ip", Физ.лицо="fiz", Самозанятый="self_employed") — обязательно
    - ИНН* (Text input) — обязательно, валидация на уникальность
    - КПП (Text input, опционально)
    - ОГРН (Text input, опционально)
    - Тип* (Select: Заказчик / Исполнитель-Поставщик / Оба) — обязательно
    - Подтип (Select, показывается условно):
      - Видимо только если `type === 'vendor'` или `type === 'both'`
      - Варианты: "Не указано" (null) / "Поставщик" / "Исполнитель" / "Исполнитель и Поставщик"
      - По умолчанию: "Не указано" (null)
    - Контакты (Textarea, опционально)
  - **Валидация:**
    - При сохранении: если указан `vendor_subtype`, но `type` не `vendor` и не `both` — показать ошибку: "Подтип можно указывать только для контрагентов типа 'Исполнитель-Поставщик'"
    - Бэкенд вернет ошибку `400`, если валидация не пройдет
  - После создания/редактирования список должен обновиться (обновить кэш запросов).
```

### ✅ Чек-лист проверки
- [ ] Отображаются данные Legal Entities (если есть в БД).
- [ ] Работает создание и редактирование компаний.
- [ ] Отображаются Счета с балансами (баланс преобразуется из строки в число и форматируется).
- [ ] Работает создание и редактирование счетов.
- [ ] Детальная страница счёта показывает историю остатков.
- [ ] Работает создание и редактирование категорий расходов.
- [ ] Работает создание и редактирование контрагентов (валидация ИНН).
- [ ] Фильтр по типу контрагента работает (Все / Заказчики / Поставщики / Исполнители).
- [ ] В таблице отображается колонка "Подтип" для vendor.
- [ ] Поле "Подтип" появляется/скрывается в форме в зависимости от выбранного типа.
- [ ] Валидация на фронте работает (нельзя выбрать подтип для customer).
- [ ] Валидация на бэкенде обрабатывается (показ ошибок при неправильной комбинации).
- [ ] Старые записи с `vendor_subtype=null` отображаются корректно.

---

## Примечание

**Важно:** Разделы про Объекты, Договоры, Платежи, Переписку, Dashboard и ТКП/МП вынесены в отдельные файлы заданий:
- Объекты → `FRONTEND_TASKS_02_OBJECTS.md`
- Прайс-листы → `FRONTEND_TASKS_03_PRICELISTS.md`
- Проекты и Сметы → `FRONTEND_TASKS_04_PROJECTS_ESTIMATES.md`
- ТКП и МП → `FRONTEND_TASKS_05_TKP_MP.md`
- Договоры → `FRONTEND_TASKS_06_CONTRACTS.md`
- Платежи → `FRONTEND_TASKS_07_PAYMENTS.md`
- Переписка → `FRONTEND_TASKS_08_COMMUNICATIONS.md`
- Dashboard → `FRONTEND_TASKS_09_DASHBOARD.md`
