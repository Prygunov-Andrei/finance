# План разработки и прогресс

## Дорожная карта разработки (по этапам)

Ниже — компактная логическая последовательность этапов.  
После каждого значимого этапа формируется **задание для проектирования фронтенда в Figma**.

### Этап 1. Фундамент backend ✅

- ✅ Аналитика и постановка задач
- ✅ Архитектура и окружение (backend + интеграция с внешним фронтендом и Figma)
- ✅ Базовые доменные модели: `Object`, `Contract`
- ✅ Платежи и реестр платежей: `Payment`, `PaymentRegistry`
- ✅ Структура импорта и логов: `ImportLog`, утилиты и базовый `BaseParser`
- ✅ Бизнес-логика cash-flow и агрегирующие отчёты
- ✅ Полный API-слой и OpenAPI/Swagger документация
- ✅ Аутентификация (JWT) и профиль пользователя (`UserProfile` + фото, медиа)
- ✅ Миграция на PostgreSQL
- ✅ Два рефакторинга (оптимизация и устранение технического долга)
- ✅ Unit-тесты (63 теста, все проходят)

**Задание для фронтенда (Figma после Этапа 1):**
- Сформировать первичный набор макетов:
  - Список объектов и карточка объекта
  - Карточка договора
  - Реестр платежей (план/факт) и детали платежа
  - Экран cash-flow/отчётов
  - Экран логина и экран профиля пользователя

---

### Этап 2. Подсистема учёта 🚧

(много юрлиц, счета, НДС, акты, оборотные средства — детально в `PROJECT_ACCOUNTING.md`)

- `TaxSystem`, `LegalEntity`, привязка договоров к юрлицам
- `Account`, `AccountBalance`, привязка платежей к счетам, расчёт остатков
- Развитие `PaymentRegistry` и `Payment` (статусы, НДС, внутренние переводы, категории расходов, финансирование)
- `Act`, `IncomingPaymentRegistry`, `ActPaymentAllocation`
- `WorkingCapitalTransaction`, баланс договора и контроль финансирования
- Аналитика: cash-flow по объектам, договорам, юрлицам, счетам и отдельный cash-flow по НДС

**Задание для фронтенда (Figma по мере реализации Этапа 2):**
- Экраны управления юрлицами и счетами, обзоры по остаткам
- Интерфейсы согласования исходящих платежей (workflow реестра)
- Реестр актов и входящих платежей, карточка акта с историей оплат
- Дашборды по балансу договоров, оборотным средствам и НДС

---

### Этап 3. SRM: задачи и протоколы совещаний 🚧

- Модели `Task` и `Meeting`
- Привязка задач к Объекту и Договору
- Поддержка подзадач, исполнителей, статусов и сроков
- Привязка задач к протоколам совещаний
- Интеграция с Telegram-ботом для уведомлений

**Задание для фронтенда (Figma после Этапа 3):**
- Канбан/список задач по объекту/договору
- Карточка задачи с историей и комментариями
- Экран протокола совещания с привязанными задачами

---

### Этап 4. Фронтенд-реализация и интеграция 🚧

Фронтенд разрабатывается **отдельно** (Figma → реализация, например, на Next.js), опираясь на REST API.

- Реализация экранов по утверждённым макетам Figma:
  - Список объектов и договоров
  - Реестр платежей и отчёты по cash-flow
  - Профиль пользователя
  - Юрлица, счета, остатки, акты, оборотные средства (после внедрения подсистемы учёта)
- Подключение визуализации cash-flow (графики, таблицы)
- Интеграция с backend через публичный HTTPS‑endpoint (на этапе разработки — временно через ngrok)

После каждого бекенд‑этапа:
- Сначала доработка/уточнение макетов в Figma
- Затем — реализация соответствующих экранов во фронтенде

---

## Текущий статус проекта

### Завершено ✅
- ✅ Фундамент backend (модели, бизнес-логика, API, аутентификация, PostgreSQL)
- ✅ Базовая интеграция с Figma через временный публичный endpoint (ngrok)

### В процессе 🚧
- 🚧 Разработка и уточнение макетов фронтенда в Figma
- 🚧 Проектирование и реализация подсистемы учёта (Этап 2)

### Запланировано ⏳
- ⏳ Реализация SRM‑модуля (задачи и протоколы)
- ⏳ Функциональное и нагрузочное тестирование
- ⏳ Настройка production‑окружения
- ⏳ Конкретные парсеры для Excel/CSV (по мере необходимости)
- ⏳ Роли пользователей и права доступа
- ⏳ Rate limiting, логирование, мониторинг, кэширование
- ⏳ Экспорт отчётов в Excel/PDF
